$ cage policy diff
policy sync — bundled v0.27.0 vs project v0.19.0

· 8 project keys equal to the bundle — in sync

dry-run — `--apply` writes adds/updates and stamps [meta] policy_version; customized values are never modified, orphans never deleted.
· bundled policy defaults are newer (v0.27.0 > v0.19.0) — run 'cage policy sync'
