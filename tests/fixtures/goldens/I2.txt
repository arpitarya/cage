$ cage insights verdict graphify
VERDICT: graphify is SAVING (GROSS) ≈ $1.3714/mo net (modeled)

  ⚠ GROSS — the cost of USING graphify is NOT subtracted (the invoking turn,
    the context it injected): no task-linked call fell within ±120s of a receipt.
    The omitted term is ≥ 0, so the true net is at most this figure.
    `cage insights compare` measures the on/off difference; `cage query gross-vs-net`.

inputs (each with its own method tag):
  · marginal saving (attrib): task 't_v4': 80,000 tok · $0.2400 (modeled)
  · roi: gross $0.9600 − own cost $0.0000 = net +$0.9600 over 4 receipt(s) (modeled)
  · net of use (task-level): INSUFFICIENT DATA — no task-linked call fell within ±120s of a receipt
  · cost drift (regression): INSUFFICIENT DATA — not enough history on both sides of the window
  · redo-rate (quality): INSUFFICIENT DATA — no task outcomes recorded
  · break-even: each receipt nets +$0.2400 on average (gross of use) — net-positive from the first receipt (derived from roi)

forward model (modeled — a bound/band, never a measured total):
graphify repo ceiling · no graphify-out/graph.json — build one (`graphify update .`) for a day-one ceiling
graphify history band · insufficient graphify history (n=4 < 5 receipts) — refusing a band over noise
  method: modeled — run more graphify queries (they file receipts at import), then re-run.

verdict composes existing views only — it computes no new statistics;
a missing input reads INSUFFICIENT DATA, never an approximation.
