> **ARCHIVED 2026-08-12 — closed by decision (Arpit), never built.** The whole
> open-work queue and every parked proposal were closed wholesale on Arpit's
> explicit instruction at v0.49.0, rather than worked. **Never built; its gate never fired** — it required fux to exist as a second real consumer. The next tool still costs what graphify cost (34 of 91 modules name it).
> **History, never spec.** Reopening this starts from a fresh reading of the code,
> not from this file — see `docs/OPEN-WORK.md` for the closure record.

---
item: TOOL-SDK
lane: parked · gated on a second tool existing
status: correctly parked — stop listing it as pending
---

# TOOL-SDK — the paved road for the next tool

The next tool should not need 34 modules. Interceptor template · `cage data meter <tool>`
· a per-tool detection registry.

[proposal](v0.49-tool-integration-contract.proposal.md), building on
[shim-contract.md](../../docs/shim-contract.md).

## The gate

**Ships only when two tools use it — fux is the second tool.** Until fux exists as a real
consumer this is a plan, not queued work. Generalising from one instance is how a paved
road becomes a rut.
