> **ARCHIVED — implemented for v0.50 (unreleased), 2026-08-14.** History, never spec:
> where this disagrees with [docs/PLAN.md](../../docs/PLAN.md) or
> [ADR 0010](../../docs/adr/0010-metric-ledgers-are-the-spend-source-forward-only-cutover.md),
> **they win**. Corrected during the build: §5.4's `est_cost_usd` premise (the real
> blocker was `provider`), the DoD's "all 43 goldens are pre-cutover" (R6 is
> future-dated by design), and §5's silence on cumulative sources and on agents with
> no metric ledger at all.

---
doc: prompt — metrics-primary: the three per-agent metric ledgers become the derive source
status: PROPOSED — unbuilt
pair: [metrics-primary.handoff.md](metrics-primary.handoff.md)
---

# Claude Code prompt: METRICS-PRIMARY — `ledger/{claude,copilot,kiro}/` become the source

**Model:** Opus — this is a substrate contract change and a read-path swap with
entanglements (26 derive sites, 7 of them id-joined). CLAUDE.md's rubric puts substrate
and irreversible calls on Opus. Do not run this on Sonnet.

**Progress:** 67% — P0 · P0a · P1 · P2 built and green 2026-08-14 (suite 1842, zero
goldens re-blessed); **P3 · P4 · P5 remaining**. All §10 questions answered, plus the
cumulative-source question P0 raised (Arpit: delta at capture — built as P0a).

You are flipping cage's derive layer off the `calls` ledger and onto the three per-agent
metric ledgers, **forward-only from a pinned cutover instant**. The full spec is
`work/metrics-primary.handoff.md` — read it first; its **Definition of done**, **Scope
(out)** and **Non-negotiables** are binding. Where this prompt and the handoff disagree,
the handoff wins.

## Before anything: three checks

1. **Concurrency.** If another session is working in this repo, STOP.
2. **The P0 STOP gate.** Run `cage doctor` and read the `copilot-metrics`, `kiro-metrics`,
   `claude-metrics` lines. If any reports **zero** collapsed rows, STOP and report — do not
   build past P0. Flipping the read path onto an empty ledger silently zeroes that agent.
3. **Sandbox.** Any manual CLI verification must set the sandbox env vars the pytest suite
   sets. A prior build polluted the real `~/.cage` by omitting them
   (`work/IMPLEMENTATION.md`, 2026-08-14). Prefer the pytest sandbox to a live run.

## Context to load first

1. `CLAUDE.md`, then `work/OPEN-WORK.md`.
2. `work/metrics-primary.handoff.md` — the spec.
3. `docs/PLAN.md` §3.1 (ground-truth spend), §3.6 (partitions/scope/team), §3.11–3.13.
4. `cage/ledger.py` · `cage/schema.py` · `cage/transcript.py` (`_fold_claude_chat`,
   `parse_calls`) · `cage/attribution.py` · `cage/receiptprice.py` · `cage/ledgersync.py`.
5. Contracts that must pass untouched: `tests/test_floor.py` · the 43 fixtures in
   `tests/fixtures/goldens/` · `tests/test_debug_coverage.py` · `tests/test_queue_honesty.py`.

## Build order — suite green after EVERY phase

- **P0 — gate + resolver skeleton.** The `CUTOVER` constant (a literal UTC instant, not
  `now()`), `ledger.spend(root, since=None)` returning pre-cutover `calls` + post-cutover
  metric rows normalized to one shape. **No derive site switched yet.** Goldens must not
  move. Report the gate result before continuing.
- **P1 — claude request-grain rows.** Extend `parse_claude_chat_metrics` to emit one row
  per folded `(requestId, message.id)`; id from `requestId`, session from the row's own
  `sessionId`. Reuse `_fold_claude_chat` — do not write a second fold. This closes
  CLAUDE-DEDUP and CLAUDE-SUBAGENT-KEY; assert the ~2× token drop on the existing dedup
  fixture rather than letting it happen quietly.
- **P2 — normalize + cost.** One row shape across the three kinds; `est_cost_usd` stamped
  at capture (never derived at read). None-sentinel credits preserved.
- **P3 — flip the aggregate family:** `report` · `chats` · `matrix` · `budget` · `forecast`
  · `roi` · `netsaved` · `quality` · `study` · `adoption`.
- **P4 — flip the id-joined family (the risky one):** `attribution` · `receiptprice` ·
  `freshness` · `provenance`/`cage why` · `commitjoin` · `commitview` · `compare` ·
  `exportcmd`'s otel render. **Also extend `ledgersync.py:71`** — it merges only
  `calls` + `receipts` into `refs/notes/cage-ledger` and would silently drop the new spine.
- **P5 — docs**, per handoff §9.5. ADR is required, not optional.

## Required workflow

1. **Explore** before writing. The handoff's §4 table was measured 2026-08-13 — if the code
   in front of you disagrees with it, STOP and report rather than adapting silently.
2. **Plan** each phase: the files you'll change and why. Pause for confirmation before P3
   and before P4 — those are the two that move user-visible numbers.
3. **Implement incrementally.** Full suite after every phase.
4. **Update docs to match** (handoff §9.5). `CLAUDE.md`: **propose the diff, do not apply it.**
5. **Verify:** full suite green, **zero goldens re-blessed**, `test_floor` untouched. A
   golden that moves is a resolver bug — fix the resolver, never re-bless.

## Constraints (hard)

- **stdlib only** — `dependencies = []`. No new third-party imports.
- **Do NOT** stop writing `calls`; **do NOT** delete, migrate, or rewrite historical rows;
  **do NOT** backfill metric ledgers from `calls`.
- **Do not modify:** `tests/test_floor.py`, `tests/fixtures/goldens/*`, existing ledger files.
- Fail-open capture; every new swallow site logs under `CAGE_DEBUG`.
- Counts-never-content: no message bodies, `summary`, or `tool-results/`.

## Acceptance criteria (self-check before finishing)

- [ ] P0 gate reported non-zero rows for all three ledgers before any flip
- [ ] `ledger.spend()` is the single derive resolver; no double-counting across the boundary
- [ ] claude request-grain rows exist; every call-id join resolves against them
- [ ] CLAUDE-DEDUP + CLAUDE-SUBAGENT-KEY closed, with the token drop asserted
- [ ] `report --usd` yields $ for all three agents post-cutover
- [ ] `ledgersync` carries the three metric kinds
- [ ] Zero goldens re-blessed; `test_floor` untouched; full suite green
- [ ] ADR written; PLAN §3.11–3.13 rewritten; `CLAUDE.md` diff proposed not applied

## Guardrails

- Ask before: anything that deletes or rewrites ledger data, changes a public CLI contract,
  or touches `cage.toml`.
- If a handoff fact turns out stale, or a requirement conflicts with what you find in the
  code, **STOP and report** — that is diagnosis, not execution, and the handoff's decisions
  were made against a measured snapshot.
- The handoff's §10 items are **answered** (Arpit, 2026-08-14): `CUTOVER =
  2026-08-14T00:00:00Z` · **no** boundary footnote (no major release has shipped on the
  old basis, so there is nothing to explain away) · kiro reads zero rather than refusing.
  The dual-write end date stays open and blocks nothing.
- **A NEW open question replaced them, raised by P0:** two metric sources are cumulative
  (copilot `cli`, kiro `cli-conv`) and cannot be spend spines without double-counting a
  straddling conversation, so those two surfaces have **no post-cutover spend**. See
  `work/IMPLEMENTATION.md` (2026-08-14, P0). **P3 cannot ship without a decision here.**
