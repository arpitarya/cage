---
doc: handoff — usage-only: delete the money subsystem, retire the cutover, one basis everywhere
status: PROPOSED — unbuilt
raised: 2026-08-14 (Arpit, across the METRICS-PRIMARY field session)
pair: [usage-only.prompt.md](usage-only.prompt.md)
---

> **ARCHIVED — implemented for v0.51 (unreleased), 2026-08-14.** History, not spec:
> where this disagrees with the living docs, they win. Shipped as **USAGE-ONLY**;
> the durable decision is [ADR 0011](adr/0011-cage-measures-usage-not-cost.md).
> Two facts in here were wrong at execution time and are corrected in the ADR: §9.5's
> claim that `work/OPEN-WORK.md` was stale (it had already been reconciled), and §5.1's
> "`spend()` = the three metric ledgers", which would have silently zeroed every
> library-, proxy- and `codex`-metered row.

# Handoff: USAGE-ONLY — cage becomes a usage meter

**One-liner:** delete dollars entirely, retire `SPEND_CUTOVER`, and make the three
per-agent metric ledgers the only basis — tokens and credits, nothing derived.

**Owner / executor:** Claude Code · **Model:** Opus (large deletion with entanglements —
CLAUDE.md rubric: *"deleting code with entanglements"*)

**Status:** Ready to build. **P0 is destructive and gated** (§2).

**Size, measured 2026-08-14:** **41 of ~70 modules** reference money. **16 delete outright
(~2,550 lines)**; the rest are stripped. `policy.py` is **NOT** deletable — 60+ files
import it; only its price surface goes. §4.5 is the change map — follow it and the build
is mechanical.

**Stress-tested:** the money deletion was decided twice and is not re-litigated. What the
handoff newly surfaced and resolved: (1) a **literal** pre-cutover wipe deletes 100% of
copilot and kiro data — every row you hold is pre-cutover — and their stores would
re-import at pre-cutover stamps and be excluded again, forever; so the boundary is removed
by **retiring the cutover**, not by deleting rows behind it. (2) Killing dollars kills
`receiptprice`, which is the only thing that made the receipt→call id mismatch dangerous —
that risk dies with the subsystem rather than needing a fix. **Residual risk:** cage loses
its only cross-agent denominator (§10, unanswered).

---

## 1. Context & background

- Field session 2026-08-14 established, against real data: claude has tokens and **no
  credit unit exists on disk**; kiro CLI has credits and **no tokens**; kiro IDE has **no
  usable store at all**; copilot has both on both surfaces.
- CLAUDE-DEDUP measured at **2.00×** across a full matched window (43,973 calls rows vs
  21,955 request rows, both spanning 2026-07-12 → 2026-08-14). The metric ledgers are
  correct; `calls` is inflated.
- METRICS-PRIMARY (v0.50) made the metric ledgers the derive source *forward of a cutover*.
  The cutover existed to protect unrebuildable `calls` history. That history is no longer
  wanted — dev phase — so the cutover's only job is done.
- Dollars were the reason for two pricing ladders, a price table, and eleven commands.
  None survive the decision.

## 2. Definition of done

**P0 STOP gate — destructive; do not proceed without an explicit go:**

- [ ] Full backup of `.cage/` and `~/.cage/` taken and its path reported **before** any
      deletion. The only irreversible phase.

Then:

- [ ] `constants.SPEND_CUTOVER` and the `calls` branch of `ledger.spend()` are **gone**.
      `spend()` reads the three metric ledgers unconditionally.
- [ ] `SPEND_SOURCES["kiro"]` no longer names `ide` (dead — no store exists); kiro's
      absence from the token spine is stated in code, like `CUMULATIVE_SOURCES`.
- [ ] **Zero `$` in any output.** Grep-gated by a new test (§9).
- [ ] All 16 §4.5-A modules deleted; all §4.5-B modules stripped; no dangling import.
- [ ] Per-agent unit policy renders with **distinct reasons**, never `0`:

      | agent | tokens | credits |
      |---|---|---|
      | claude | value | `—` *"Claude Code records no credit unit on disk"* |
      | copilot | value | value |
      | kiro | `—` *"no IDE token store on this install"* | value |

- [ ] kiro credits retagged `estimated` → `measured`.
- [ ] `cage doctor`'s kiro-ide line distinguishes **db absent** / **table missing** /
      **column drift** instead of one indistinguishable zero.
- [ ] Full suite green after every phase. Goldens re-blessed **only** where a deleted `$`
      column is the cause, each named in the commit message.
- [ ] `work/OPEN-WORK.md` reconciled (§9.5) — it currently contradicts the archive.

## 3. Scope

**In scope:** the money subsystem's deletion, the cutover's retirement, the unit policy,
the honesty fixes, docs and queue reconciliation.

**Out of scope (explicit) — do NOT do these:**

- **Do not delete `policy.py`.** 60+ modules and tests import it. Remove only its price
  surface (§4.5-B).
- **Do not delete pre-cutover metric rows.** They are the *corrected* history (21,900
  request rows back to Jul 12). Retiring the cutover makes them readable again — the point,
  not a side effect.
- **Do not re-denominate** the deleted commands in tokens. Deleted, not converted.
- **Do not stop writing `calls`.** It remains the id namespace savings receipts reference
  (`metering.record_call` writes `call=<c_ id>`); `adoption` and `commitjoin` still resolve
  against it. It is simply no longer a *derive* source.
- **Do not add a dependency.** `dependencies = []`.
- **Do not touch `tests/test_floor.py`.**
- Do not build the `*-READ` / `*-CSV` views (still parked).

## 4. Current state

| fact | value |
|---|---|
| modules referencing money | **41 of ~70** |
| `ledger.spend()` already adopted in | report · chats · budget · forecast · adoption · commitview · compare · freshness · exportcmd |
| still on `ledger.calls()` — correctly | importcmd (capture seen-sets) · ledgersync (union) · demo (writes) |
| still on `ledger.calls()` — to review | doctorcmd ×4 · exportcmd ×2 · clicmds · hookcmd · mcpserver · pricescmd · taskcorr |
| claude rows | 21,955 request (all with `provider`), Jul 12 → Aug 14 |
| copilot rows | 57 chat · 27 cli · 26 cli-delta — **all pre-cutover** |
| kiro rows | 3 cli-conv · 5 cli-turn · 3 credits — **all pre-cutover**, zero `ide` |
| suite | 1842 passed / 11 skipped · 43 goldens · 100 test files |

**Read first:** `CLAUDE.md` · `work/OPEN-WORK.md` (**stale — see §9.5**) ·
`work/archive/v0.50-metrics-primary.handoff.md` · `cage/ledger.py` · `cage/policy.py`.

**Field evidence (do not re-derive):**
`work/research/2026-08-13-kiro-per-chat-usage-fetch-spec.md`.

## 4.5 Change map

### A — Delete outright (money-only, ~2,552 lines)

| module | lines | its own docstring says |
|---|---|---|
| `cage/pricescmd.py` | 494 | the `cage prices` command group |
| `cage/pricestoml.py` | 460 | `prices.toml` parsing |
| `cage/verdict.py` | 273 | pure composer over `roi`/`netsaved`/`attribution` |
| `cage/creditprice.py` | 217 | "the copilot pricing ladder — recorded credits × your rate" |
| `cage/netsaved.py` | 193 | "gross vs net savings — minus what it cost to use" |
| `cage/receiptprice.py` | 133 | "the pricing ladder for call-less token receipts" |
| `cage/roi.py` | 128 | "gross saved $ per tool vs the tool's own cost" |
| `cage/matrix.py` | 126 | "the counterfactual permutation table — what every stack would cost" |
| `cage/estimate.py` | 95 | "a pre-task **cost** band from matching closed tasks" |
| `cage/prices.py` | 84 | "cost arithmetic — token counts + a price row → USD" |
| `cage/quality.py` | 72 | "quality-adjusted **cost**" |
| `cage/convert.py` | 65 | "the single unit→USD dispatch for a savings receipt" |
| `cage/forecast.py` | 61 | "project monthly **spend**" |
| `cage/budget.py` | 59 | "ceilings per session/day — sums recorded spend" |
| `cage/regression.py` | 54 | "alert when **cost-per-call** drifts up" |
| `cage/recommend.py` | 38 | "enable every tool whose net saving (saved $ − cost) is positive" |

**Two need a decision before deletion (§10):** `quality.py` — the *outcome signal* (did the
task succeed?) is useful without cost; only the cost pairing must die. `estimate.py` — the
median/IQR machinery is unit-agnostic. Both are currently listed as full deletes per
"delete, not convert"; confirm or carve out.

### B — Strip money, keep the module (highest-density first)

| module | money refs / lines | what to remove |
|---|---|---|
| `cage/report.py` | 226 / 894 | `--usd` path, cost columns, UNPRICED summary; keep token/credit render |
| `cage/explain_data.py` | 187 / 1538 | every pricing concept entry (`est_cost_usd`, ladders, rungs) |
| `cage/policy.py` | 78 / 487 | **only** `price_match` + `[prices]` load. **Never delete the module.** |
| `cage/doctorcmd.py` | 69 / 1185 | price-staleness + unpriced checks; also §4.5-D |
| `cage/cli.py` | 61 / 935 | six `--usd` flags + eleven subparsers (§4.5-C) |
| `cage/chats.py` | 61 / 569 | cost column + repricing; the credits bucket stays (§4.5-D) |
| `cage/initcmd.py` | 55 / 248 | price scaffolding in `cage setup` |
| `cage/freshness.py` | 39 / 161 | `receiptprice.eligible`/`resolve` call sites |
| `cage/paths.py` | 39 / 1383 | prices-file paths |
| `cage/attribution.py` | 38 / 145 | pricing; **keep** token-denominated savings + the id join |
| `cage/importcmd.py` | 35 / 1500 | `est_cost_usd` in the manifest write (l. 1114) |
| `cage/study.py` | 34 / 412 | cost columns in the fleet report |
| `cage/clicmds.py` | 29 / 913 | eleven `cmd_*` handlers for the deleted commands |
| `cage/compare.py` | 26 / 190 | `creditprice` use |
| `cage/schema.py` | 23 / 702 | **keep** `est_cost_usd` field (append-only), read nowhere |
| `cage/constants.py` | 23 / 311 | `SPEND_CUTOVER` (l. 311) + price constants |
| `cage/policysync.py` | 22 / 387 | price-table migration |
| `cage/otelout.py` | 22 / 149 | cost attributes; **keep** the export |
| also | | `provenance.py` (l. 32 cost line) · `metering.py` (l. 54–55) · `manifest.py` (l. 83, 94) · `render.py` (`usd` l. 67, `signed_usd` l. 71 — **keep `tok` l. 76**) · `display.py` · `taskgroup.py` · `commitview.py` · `exportcmd.py` · `mcpserver.py` · `hookcmd.py` · `serve.py` · `graphifychat.py` · `runstamp.py` · `transcript.py` |

### C — CLI surface to remove (`cage/cli.py`, line anchors)

- `--usd` flags: **l. 144, 165, 302, 338** (+2 more — grep `"--usd"`).
- Subparsers + their `set_defaults`: `matrix` **297–309** · `roi` **311–317** ·
  `verdict` **391–399** · `budget` **401–407** · `estimate` **422–434** ·
  `forecast` **452–456** · `regression` **458–464** · `recommend` **466–471** ·
  `quality` **502–506** · `prices` group **565–585**.
- Each has a matching `clicmds.cmd_*` handler to delete.

### D — The three targeted fixes

| what | anchor | change |
|---|---|---|
| cutover retirement | `constants.py:311` `SPEND_CUTOVER`; `ledger.py:~498` `spend()` | delete constant + the `calls` branch; `basis` becomes constant or drops |
| kiro dead pointer | `ledger.py:~464` `SPEND_SOURCES` / `~473` `CUMULATIVE_SOURCES` | `"kiro": ("ide",)` → stated absence with the reason |
| kiro credits retag | `schema.py` `make_credit(… method: str = "estimated")` | default → `"measured"`; update the docstring's rationale |
| doctor three-way | `doctorcmd.py:332` `_kiro_metrics()`, registered at **l. 1147** | split *db absent* / *table missing* / *column drift* |
| N/A render | `chats.py:174` `_bucket_key`, bucket dict **~l. 198** (`"credits": None`) | two distinct reason strings; reuse the `—`-never-0 idiom at **l. 422** |

### E — Test impact (~30 files)

**Delete with their module:** `test_pricing` · `test_prices_cli` · `test_prices_split` ·
`test_pricestoml` · `test_receipt_pricing` · `test_netsaved` · `test_verdict` ·
`test_convert` · `test_estimate`.
**Strip money assertions:** `test_views` · `test_features` · `test_legacy_ledger` ·
`test_adoption` · `test_attribution` · `test_report_credits` · `test_report_savings` ·
`test_report_cache_split` · `test_copilot_credits` · `test_freshness` · `test_policysync` ·
`test_output_spec` · `test_hooks_layer` · `test_graphify_usage` · `test_cli_reference`.
**Rewrite:** `tests/test_spend_resolver.py` — **l. 37–45 pin `SPEND_CUTOVER`'s literal
value and its declaration form**; those assertions must become "no cutover exists".

## 5. Technical approach (decided)

### 5.1 Retire the cutover — do not wipe behind it

- Delete `SPEND_CUTOVER` and `spend()`'s `calls` branch. `spend()` = the three metric
  ledgers, filtered by `SPEND_SOURCES`.
- **Why not a literal pre-cutover wipe** (the phrasing of the decision): every copilot and
  kiro row in existence is pre-cutover, so a literal wipe zeroes both agents — and their
  stores (VS Code `chatSessions`, `kiro-cli/data.sqlite3`) persist, so re-import lands
  pre-cutover stamps and is excluded again. Retiring the cutover achieves the stated goal —
  **no boundary, one basis, nothing to footnote** — and keeps corrected history.

### 5.2 Delete money, don't convert it

- **Keep** `savings/` receipts and attribution — savings are token-denominated and survive
  intact. Only their *pricing* dies.
- **Keep** `make_call`'s `est_cost_usd` field (append-only law), read nowhere.

### 5.3 Units are per-agent, never summed across agents

`credits` is not one unit: copilot's is GitHub's tokens×rates computation, kiro's is an AWS
credit. One column is fine; **summing or ranking across agents is forbidden** and must be
prevented in code, not convention. Tokens are likewise not cross-comparable — copilot
carries no `cached_in` at all on the vscode surface (0 of 57 rows).

### 5.4 N/A carries a reason, never a zero

Reuse the existing idiom (`chats.py:422` — *"`—` is never 0%"*). Two absences, two
different reason strings — one is a vendor law, the other a missing store. Rendering them
identically invites a future agent to "fix" the wrong one.

## 6. Non-negotiables

- **stdlib only** — `dependencies = []`.
- **Append-only** — never rewrite or delete a ledger row outside P0's gated wipe.
- **Fail-open capture**; every new swallow site logs under `CAGE_DEBUG`.
- **Counts-never-content.**
- **None-sentinel credits preserved** — a recorded `0.0` and an absent value stay distinct.
- **Do not touch:** `tests/test_floor.py`; goldens except where a removed `$` column forces
  it (name each).
- **`CLAUDE.md` edits are PROPOSED, never applied.**
- **No concurrent session** in this repo.

## 7. Dependencies & prerequisites

- A verified backup (P0 gate). No services, env vars, or secrets.
- Manual CLI verification must use the pytest sandbox env vars — a prior build polluted the
  real `~/.cage` by omitting them (`work/IMPLEMENTATION.md`, 2026-08-14).

## 8. Edge cases & risks

| risk | handling |
|---|---|
| `receiptprice` has **11 importers**, `netsaved` 6, `roi` 5 | Delete leaf-first: `convert` → `prices`/`creditprice`/`receiptprice` → `roi`/`netsaved`/`matrix` → `verdict`/`recommend` → the commands. Suite green between each. |
| `policy.py` looks deletable (78 money refs) — **it is not** | 60+ importers. Price surface only. |
| Goldens carry `$` columns | Re-bless only those; name each. Any other golden moving is a bug. |
| `cage.toml` has no `[[prices]]` in-repo (184 lines, `[meta]`…`[[sources.*]]`) | Prices live in the bundled/global policy via `pricestoml`. Confirm before editing config schema; a stale table must not fail a load. |
| copilot/kiro become readable once the cutover dies | Expected and desired. |
| A future Kiro adds `devdata.sqlite` | Keep `parse_kiro_ide_metrics`; the doctor's three-way check announces the flip. |
| Orphaned zero-byte `.tmp` in `~/.cage/ledger/` | Observed 2026-08-14; clean up on write. |

## 9. Testing & validation

- **The deletion's own regression pin:** a grep-gated test that `$`, `usd`,
  `est_cost_usd`, `price` appear in no output path (mirror `test_queue_honesty`'s style).
- New: `spend()` with no cutover (one basis, no double-count); the three-way kiro-ide
  doctor check; the two N/A reason strings; a guard that credits are never summed across
  agents.
- Regression: `test_floor` untouched · `test_debug_coverage` · `test_queue_honesty` ·
  `test_output_spec` · `test_cli_reference`.
- Suite green after **every** phase and **every leaf deletion**.

## 9.5 Documentation impact

- [ ] **ADR (required, new)** — why cage stopped measuring money; why the cutover was
      retired rather than wiped behind. The decision a future agent would reverse.
- [ ] **`docs/PLAN.md`** — §3.1 *"ground-truth spend"* · §3.3 policy/prices · §3.8 · §4.5
      · §6 · §7 · §8.1–8.5 are all money. Rewrite or mark superseded.
- [ ] **`README.md`** — the product is a usage meter, not a cost meter.
- [ ] **`docs/FORMULAS.md`** — ~120 citations; every cost formula goes.
- [ ] **`docs/kiro-capture.md`** — still lists `devdata.sqlite` as a live source. Field
      probe 2026-08-14: `dev_data/` holds only `tokens_generated.jsonl` — 28 rows,
      1,576 in / **0 out**, model `"agent"` on every row, with a byte-identical 6-row block
      repeated (not summable).
- [ ] **`work/research/2026-08-13-kiro-per-chat-usage-fetch-spec.md`** — close the
      `unverified:` header: probe 2 answered, no sqlite twin exists.
- [ ] **`work/MACHINE.md`** — split `| Kiro installed | yes |` into IDE and CLI rows.
- [ ] **`CHANGELOG.md`** · **`docs/CLI.md`** (eleven commands vanish) · **`docs/GLOSSARY.md`**
      · **`work/DOC-REGISTRY.md`** · **`docs/README.md`**.
- [ ] **`CLAUDE.md`** — substrate/ledger description. **PROPOSE, do not apply.**
- [ ] **`work/OPEN-WORK.md`** — lists CLAUDE-DEDUP, CLAUDE-SUBAGENT-KEY, six
      `*-READ`/`*-CSV` items and METRICS-PRIMARY as pending while
      `work/archive/v0.50-metrics-primary.*` says implemented. Reconcile against git.

## 10. Open questions

- **OPEN QUESTION (Arpit) — the one thing not yet decided.** Dollars were the only unit
  making claude, copilot and kiro comparable. After this, "which agent used more" has no
  answer: claude has no credits, kiro has no tokens, tokens aren't cross-vendor comparable.
  Flagged twice in session, unanswered.
- **OPEN QUESTION:** `quality.py` and `estimate.py` — full delete, or keep the
  unit-agnostic half (outcome signal / median-IQR)? §4.5-A currently says delete.
- **OPEN QUESTION:** truncate the inflated `calls` history in P0, or leave it unread?
  Recommend **leave it** — costs nothing, and it is the id namespace receipts point at.
- **OPEN QUESTION:** does `cage` keep its spend-tool name and positioning? §9.5's README
  item depends on the answer.
