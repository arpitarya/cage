> **ARCHIVED — resolved for v0.39 (unreleased), 2026-08-01.** Proposal 1
> (`prices.toml` split) **IMPLEMENTED** verbatim — living spec: `CLAUDE.md` itself.
> Proposal 2 (`[sources]` authority) **DECLINED**, independently re-verified against
> `paths.resolve_log_sources`'s docstring at execution time (same conclusion as this
> handoff). Zero code changes. Execution pair archived beside it:
> [prompt](v0.39-claude-md-sync.prompt.md). **History, never current spec.**

---
doc: handoff — CLAUDE.md sync (CMD-SYNC)
status: decided 2026-08-01, unbuilt
scope: ONE of the two parked proposals applies; the other is WRONG
---

# Handoff — CMD-SYNC: apply one proposal, decline the other

**The headline: only proposal 1 is correct. Proposal 2 would make CLAUDE.md lie about
the code.** Verified against source, not against the proposals' own quotes.

**Parallel execution: NO.** Both edit `CLAUDE.md`, and after this review only one of
them has work in it anyway. See the last section.

---

## Proposal 1 — `prices.toml` split — **APPLY** ✅

[claude-md-prices-file.md](v0.39-claude-md-prices-file.proposal.md)

**Verified true.** `cage/data/prices.toml` exists, `policy._BUNDLED_PRICES = "prices.toml"`
loads it, and `_bundled()` merges the two files. **CLAUDE.md mentions `prices.toml`
exactly zero times**, and its flow diagram still reads:

```
cage.toml (prices/order/budgets)       → report · attrib · matrix · budget · roi
```

which names prices as living in `cage.toml`. That is wrong on a load-bearing fact — the
file every agent loads tells them the wrong home for the vendor rate card.

**Do:** apply the proposal's edits — the flow diagram, its caption, and the Must-Know
bullets — so `cage.toml` (order/budgets/routing) and **`prices.toml`** (model prices,
`[credits]`) read as the two config inputs they are. Governing rule from the proposal,
worth keeping verbatim: **vendor facts move, routing decisions stay.**

## Proposal 2 — `[sources]` authority — **DECLINE** ❌

[claude-md-sources-authority.md](v0.39-claude-md-sources-authority.proposal.md)

It asks CLAUDE.md to say `[sources]` is the **only** source of log paths, with no
built-in runtime fallback, and that an empty/absent `[sources]` "captures nothing,
loudly".

**The code says the opposite.** `paths.resolve_log_sources`'s own docstring:

> Precedence per built-in agent: **env override > policy `[sources]` > built-in**.
> A `[sources.<agent>] paths = [...]` *adds* candidates; `replace = true` drops that
> agent's built-ins first. … **Fully additive: an empty/absent `[sources]` returns
> exactly the built-in registry.**

That is **exactly what CLAUDE.md already says** (lines ~737, ~743). CLAUDE.md is correct;
the proposal describes a Directive A end-state that **never fully shipped** — the
`sources_seed` / `sources_drift` *machinery* exists, but the runtime fallback was never
removed.

**Applying it would replace a true statement with a false one** in the file every agent
loads. Decline it, and archive it with the reason.

> **My error, recorded:** earlier today I reported both proposals "verified still needed
> — CLAUDE.md stale on both". I had checked that CLAUDE.md's wording matched the
> proposal's *quoted* "current text" and inferred staleness. **I never checked the
> code.** Proposal 2 is not stale documentation; it is an unbuilt design.

**Residual, worth filing:** if Directive A is still wanted, that is a *code* change
(remove the fallback, make no-`[sources]` loud) — a real feature, not a doc sync.
Nothing suggests it is wanted; file it only if you say so.

---

## Can the two run in parallel? **No — and it's moot**

- **Same file.** Both rewrite overlapping regions of `CLAUDE.md` (the flow diagram and
  its caption, the Must-Know/Adapters bullets). Two agents editing one dense reference
  file is a guaranteed conflict for zero speedup.
- **After this review there is only one job** — proposal 1. Parallelism has nothing to
  parallelise.
- **Against CODEX-OUT / DEBT / OTEL:** those are done or independent, but note DEBT and
  CODEX-OUT *both* touched CLAUDE.md today. Re-read it before editing; do not work from
  a remembered copy.

## Definition of done

- [ ] Flow diagram + caption name **both** `cage.toml` and `prices.toml` correctly
- [ ] Must-Know bullets updated per proposal 1; "vendor facts move, routing decisions
      stay" preserved as the governing sentence
- [ ] `grep -c "prices.toml" CLAUDE.md` > 0 (it is 0 today)
- [ ] **Proposal 2 NOT applied**; archived as **declined**, header naming the decider,
      the date, and `paths.resolve_log_sources`'s docstring as the contradicting evidence
- [ ] Proposal 1 archived as implemented per the lifecycle
- [ ] No code changes at all — this is a documentation sync
- [ ] `just test` green (docs-only; a golden could pin a docstring)
- [ ] IMPLEMENTATION / WORKLOG / DOC-REGISTRY / OPEN-WORK updated

## Out of scope

Building Directive A · touching `paths.py` · any code change.
