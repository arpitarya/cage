> **Archived run record** — executed against cage 0.22.1 on 2026-07-11/12; the evergreen plan lives at [docs/full-test-plan-sibling-repo.md](../full-test-plan-sibling-repo.md). History, not spec.

# Cage full test run — findings — v0.22.1

**Version under test:** cage 0.22.1 (editable uv-tool install → `/Users/arpitarya/my_programs/cage` working tree)
**Date:** 2026-07-11
**Runner:** Claude Code (driver prompt: `docs/cage-claude-code-prompt-full-test-run.md`), manual cells by Arpit.
**Real-ledger baseline (`~/.cage/ledger/`):** calls-2026-06 = 12,762 rows · calls-2026-07 = 4,813 rows · receipts-2026-06 = 7 rows · policy.toml md5 d2ac536375975c6141923c5545a75fa7. NOTE: the cage repo itself has no project `.cage/`, so this driver session's own Claude hooks import into the **global** ledger — expected growth in calls-2026-07 during the run; delta reconciled at close.
**Note:** originally kept in `../cage-testbed/manual/` per the driver prompt; the user deleted that stale v0.16 testbed mid-run, so the record was recreated here (`cage-testbed-v0.22.1/manual/`) from the in-context copy. No findings were lost.

| # | Part/step | Agent×surface | Expected | Actual | Severity | debug.log line? |
|---|---|---|---|---|---|---|
| 1 | A: `just test` | — | all pass | **PASS** — 569 passed, 1 skipped (platform-conditional), 16.5s. Env note: bare `just test` fails on this machine — pyenv exposes no `python3.14` shim in non-interactive zsh; ran via `CAGE_PYTHON=~/.pyenv/versions/3.14.2/bin/python3.14 just test`. Environment issue, not a cage bug. | n/a (env note) | n/a |
| 2 | A: dummyrepo S1–S13 | — | all PASS | **PASS** — 13/13 scenarios PASS (wiring, fixtures→exact rows, fail-open, PII, compare/estimate/calibration/verdict goldens, fleet, attention, pricing, launcher mode, pyz parity) | — | — |
| 3 | A: skillgen `--check` | — | no drift | **PASS** — "check OK: 4 artifact(s) match" | — | — |
| 4 | A: bare-venv import | — | import succeeds, no deps | **PASS** — fresh 3.14.2 venv, `pip install <tree>` (no extras), `import cage` OK, no third-party packages pulled | — | — |
| 5 | A: `cage demo` | — | §4.4 tables reproduce | **PASS** — attrib 41,400 tok / $0.1242; matrix $0.1725→$0.0483 (72%), all exact vs plan §4.4. Run under scratch `CAGE_BASE` to protect the real ledger. Note: demo tags only ✓✓✓ `measured` (one recorded invoice) where the plan's illustrative table shows extra `measured` rows from its multi-run hypothetical — method law, by design, numbers identical. | — | — |
| 6 | A: toolchain note | — | working tree under test | **INFO** — PATH `cage` = uv tool `cage-flux`, editable → this working tree (dist-info stamped 0.12.0, stale but cosmetic; runtime reports 0.22.1) | paper-cut (env) | n/a |
| 7 | B: scaffold + `cage init` | — | project .cage created | **PASS** — fresh `cage-testbed-v0.22.1` (old `cage-testbed` was retained for user deletion; user deleted it mid-run). Note: bare non-interactive `cage setup` prints the agent-pick usage + exit 2 (wizard is TTY-only) — used `cage setup --all` per plan intent. | — | — |
| 8 | B: `cage setup --all` ×2 idempotent | all 4 agents | 2nd run byte-identical | **PASS** — 20 files hashed (project wiring + user-level + .git/hooks): run 2 byte-identical to run 1. Run 1 did update user-level `~/.codex/config.toml` (MCP `command=/Users/arpitarya/.local/bin/cage` — absolute by design for user-level); `~/.copilot/hooks/cage.json` + `~/.claude/settings.json` untouched (already current). | — | — |
| 9 | B: `cage doctor` baseline | — | ok/warn only | **PASS** — all ✔; one `·` note: `bin/graphify` not on PATH (fresh shell, optional wiring). Snapshot: `manual/doctor-0-v0.22.1.txt` (recreated from run output after testbed deletion). Portability check names mode `standard`, shim resolves → 0.22.1. | — | — |
| 10 | C: `cage doctor --paths` | all 4 (sandboxed homes) | per-candidate found/missing + why, sink chain, nothing written | **PASS** — every candidate listed with file/row/not-yet-imported counts; copilot's missing `workspaceStorage` candidate carries `why: no files match */chatSessions/*.jsonl`; env-override provenance printed (`CAGE_VSCODE_USER: set`, `KIRO_DATA_DIR: set`); active ledger + full precedence chain at the end; exit 0; no `.cage/state` created. **Planted logs** (fixtures via env overrides — parser check, NOT live capture). | — | — |
| 11 | C: export sweep (v0.19) | all 4 (planted) | export imports first | **PASS** — empty ledger + 4 planted fixture logs → `cage export` printed `↻ imported 8 new call(s)` then all 8 rows; month-sharded correctly (6→2026-06, 2→2026-07). `--no-import` on a freshly appended kiro row → row absent from output, ledger unchanged. | — | — |
| 12 | C: capture switch | — | CAGE_CAPTURE=0 blocks, =1 restores | **PASS** — `=0` → "capture disabled … import skipped", 0 rows; `=1` → row lands (kiro 1 call). Incremental cursors: other agents rescanned 0 files. `[capture] import_before_export = false` in policy → `cage export` skipped the sweep (appended row absent); removed the policy line, `cage import` converged. | — | — |
| 13 | C: gap_ms (v0.18) | claude vs rest (planted) | claude rows carry gap_ms, others never | **PASS** — claude row 2 `gap_ms=37000`, claude row 1 absent (no prior turn); all codex/copilot/kiro rows absent-explicit. `cage human` renders the derived block: "derived (turn-gaps, capped) · cap 10 min · estimated — reference only, never summed with attested"; 37s → 0.6167 min exact. | — | — |
| 14 | C: pre-manual baseline | — | — | **INFO** — testbed ledger before manual cells: 10 planted rows, `cage report` = 10 calls / 48,862 in / 3,557 out / $0.1906, one family-match note (`gpt-5.1-codex → gpt-5` ≈ priced-by-family, correctly NOT rendered as exact). | — | — |
| 15 | C: Claude Code CLI (live) | claude×cli | rows land at hook time, no manual import | **PASS** — session `a1be494b`: 18 calls landed **live** (present before any manual import), `agent=claude-code`, `project=cage-testbed-v0.22.1`, non-zero tokens, priced by family (`claude-haiku-4-5-20251001 → claude-haiku-4-5`, ≈-labelled). SessionStart wiring is `import-claude --project .` (slug-scoped) + live Stop/SessionEnd capture — explains zero cross-project pollution from hooks. | — | — |
| 16 | C: Claude commit → provenance | claude×cli | `hooked` row; `cage origin` explains | **PASS** — commit `7cb9310` ("add hello") → 2 `hooked` rows conf 1.00 (`src/hello.py`, repo-relative, no content) + 2 `transcript` fallback rows conf 0.87 at SessionEnd; `cage origin 7cb9310` → `origin=agent · confidence=1.0`, full row table. `cage verify` → 0 warnings, **exit 0**. | — | — |
| 17 | C: Claude Code VS Code ext | claude×vscode | hooks honored; import catches stragglers | **PASS** — session `453228b6`: 30 calls, ~28 landed live via hooks (extension honors hooks), last ~2 turns caught up by `cage import`. Carries `gap_ms` on 2 rows (live derived-attention capture works). | — | — |
| 18 | C: Codex CLI | codex×cli | — | **N/A (not run by user).** Partial coverage via planted fixture: `cage limits` state populated from fixture `rate_limits` → `.cage/state/limits.json` correct shape (windows 10080/43200, used_percent, resets_at) — labelled PLANTED, not live. | — | — |
| 19 | C: Codex VS Code ext | codex×vscode | find real log; rows import | **PASS** — extension writes the **same** `~/.codex/sessions/**/rollout-*.jsonl` as the CLI; today's session = 5 calls, `gpt-5.5`, landed via universal `cage import` only (no codex hook fired — user skipped codex CLI). Existing `codex/vscode` fixture format held (fixtures README carries no UNVERIFIED-FORMAT flags — all field-verified as of v0.22); no re-capture needed. | — | — |
| 20 | C: Copilot CLI | copilot×cli | user-level hook sweeps all agents | **PASS** — session `adbc264f` in `~/.copilot/session-state/<id>/events.jsonl`, 1 call, `claude-haiku-4.5` → family-priced (`.`↔`-` fold worked). Multi-agent rows (claude/kiro) were present in the testbed ledger before any manual import — hook-time all-agent sweep confirmed. | — | — |
| 21 | C: Copilot VS Code chat | copilot×vscode | chat log located; rows import | **PASS** — session `91447005` in VS Code `workspaceStorage/<hash>/chatSessions/*.jsonl` (the registered second candidate), 1 call, model `copilot/auto` → **UNPRICED ⚠** (correct: a router/auto model is never silently defaulted; `cage prices unpriced` names it with a fix line). Existing `copilot/vscode` fixture format held. | — | — |
| 22 | C: Kiro IDE ×2 sessions | kiro×ide | no duplicate rows on self-backfill | **PASS** — 16 live rows from 2 sessions (agentStop self-backfill), `cage import` run twice → 0 new, count stable at 20 (incl. 4 planted). Coarse fidelity confirmed (model=`agent`, `tokens_out` often 0) — noted, matches spec. Kiro CLI: **N/A (not installed/run)**. | — | — |
| 23 | C: PII canary sweep | all cells | canary nowhere in any cage footprint | **PASS** — `zanzibar-marmalade-vector` grep: testbed `.cage/` clean, global `~/.cage/` clean, ledger content grep (`add hello`/`greet()`/`farewell`) clean; canary present only in the source transcripts (expected). Provenance carries repo-relative paths only. | — | — |
| 24 | C: universal import + dedupe | all | import idempotent | **PASS** — bare `cage import` swept the whole stack (27,428 claude + 373 codex + 57 copilot rows incl. full history — pull-based global by design; `project` field keeps slices separable); immediate re-run → 0 new rows across all four agents. `doctor` shows per-agent "hook wired — CLI-only, may not fire" + universal path + last-import ago. | — | — |
| 25 | C: tasks.jsonl absent after capture | claude×cli | (investigated) | **INFO — expected** — `hooks._snapshot_tasks` records a task row only for call rows carrying a `task` id; transcript capture stamps none. Task rows appear via `cage outcome` (Part E). Not a bug. | — | — |
| 26 | D: report + variants | — | render, self-consistent, exit 0 | **PASS** — `report`, `--project` (testbed slice + honest claude-only caveat), `--since 7d` (month-shard cutoff respected), `--team` (renders; see #27b), `--by` families in help. Family notes list every normalized match (incl. `copilot/` route-prefix strip); UNPRICED ⚠ named the router key. | — | — |
| 27 | D: `--since` takes a window, not a date | — | plan says `--since <date>` | **PLAN DRIFT** — implementation accepts `7d/24h/2w` windows only; a date string raises the typed `error: invalid --since '2026-07-05' — use a window like 7d, 24h, or 2w` (exit 1). Correct behavior, stale plan text. Proposed plan edit: Part D bullet → "`--since <window>` (7d/24h/2w; cutoff respected across month shards)". | paper-cut (plan doc) | — |
| 27b | D: `--scope` / `--team` empty-state wording | — | scoped empty message | **PAPER-CUT** — `report --scope src` over a ledger whose rows all have empty `scope` prints "no calls recorded yet — meter some traffic first" (misleading: 28k calls exist, just none in scope); `report --team` with no `refs/notes/cage-ledger` gives no hint it's local-only. Both cosmetic. | paper-cut | — |
| 28 | D: attrib / matrix / matrix --human | — | anchor behind flag; no-flag byte-identical | **PASS** — attrib t-manual-1: graphify 4,776 tok `modeled`; matrix: recorded run `measured`, counterfactual `modeled`, honest `$0.0000` under an unpriced joined model; `--human` adds the anchor row (`—/estimated/$60` + vs-human columns); no-flag output byte-identical across runs and unaffected by human receipts. | — | — |
| 29 | D: budget/roi/recommend/forecast/regression/quality/why/limits | — | render + method tags | **PASS** — budget warns `⚠ OVER` (day $511.99 vs $25 cap); forecast projects + ceiling warning; regression flags cost/call drift (`measured`); quality renders the honest "no ok tasks" metric; `why <call-id>` renders the call card with derive-time cost; limits shows codex quota snapshot + credits section with **no numbers** ("no multiplier configured") — credits off by default confirmed. | — | — |
| 30 | D: human axis + rate provenance | — | attested vs derived, env wins | **PASS** — `human-record --minutes 45` → attested $60 @ policy $80/hr, conf 0.70, `estimated`; `CAGE_HUMAN_RATE=200` → header "rate source: env ($200/hr)", $150 — provenance printed, never a hard-coded rate. Attested and derived render as separate blocks "never summed with attested". `calibration --human` refuses below min-n with actionable guidance. | — | — |
| 31 | D: `cage outcome` has no `--ok` flag | — | plan says `--ok/--redo` | **PLAN DRIFT** — success is the default (no flag), only `--redo` exists; S6 goldens pass against this CLI. Proposed plan edit: "cage outcome <task> [--redo] --label <word>". Label validation: path → typed error exit 1; two words → rejected; valid token → task row with git snapshot (sha/branch/counts/top-level dirs only — PII guard held). | paper-cut (plan doc) | — |
| 32 | D: origin attest / notes-sync / ledger-sync | — | dry-run defaults | **PASS** — `origin --attest human` writes the heuristic+human pair; `notes-sync` → "dry-run — 1 sha(s)… Set CAGE_NOTES_WRITE=1 (CI) or --write"; `ledger-sync` → dry-run, 27,911 rows ready. No git notes written on this machine. | — | — |
| 33 | D: query + --help | — | 3 modes + grouped help | **PASS** — calculation ("how is roi calculated") interpolates live formula + code refs; concept ("how does cage work") interpolates live paths/subcommand-count/4 agents + plan anchor; nonsense → closest-topic suggestions, never a guess; `--help` grouped with a query pointer footer. | — | — |
| 34 | D: graphify receipt path | — | receipts honest | **PASS** (mechanism) — tiny cited file → NO receipt (`actual >= raw` → "stay honest"); big cited file → `graphify tokens 4,776 modeled conf 0.6` receipt bound to `--task`. Fail-open verified. | — | — |
| 35 | D: **graphify double-metering** | — | one query = one receipt | **BUG → FIXED IN TREE** — graphify v0.5.0 *natively* files a cage receipt (task = project dirname) on every query when `.cage/` exists; `cage graphify -- …` (and thus the setup-installed `bin/graphify` interceptor) filed a **second** receipt for the same query → savings counted 2× in aggregate views. **Fix (wrapper-side detection, cage-only, works for every graphify version):** `graphifymeter.run` snapshots the ledger's graphify receipt ids before the child runs; if the child filed its own, the wrapper defers (one saving, one receipt). The child also gets `CAGE_GRAPHIFY_METERED=1` — a forward handshake: a graphify that respects it skips its native receipt and the wrapper's task-bound receipt wins (task binding returns). Old graphify (no native shim) keeps the wrapper's receipt exactly as before; pre-existing receipts never suppress (dedupe keys on rows NEW during the run — regression-tested). 3 new tests in `tests/test_graphifymeter.py`; end-to-end re-run in the testbed: one wrapped query → **+1 receipt** (was +2). Until graphify adopts the handshake, a wrapped query's surviving receipt carries the dirname task, not `--task` — noted, honest-totals > task-binding. Suggested graphify-side follow-up: respect the env var in its shim. | **bug (fixed)** — was inflating savings 2× | — |
| 36 | D: pricing workflow (v0.19) | — | full loop + retroactive | **PASS** — `unpriced` names `—/copilot/auto` with a ready-to-run alias fix line; `alias` → same historical row repriced $0 → $0.0258, report renders "≈ priced by alias … never exact", ⚠ cleared; `set` = surgical 5-line cage-managed block only (diff-verified), family note vanishes, history repriced exact ($0.0116→$0.0128 at the new rate); `sync` dry-run + "bundled prices are newer" recommendation with aged project meta; ledger bytes never rewritten. | — | — |
| 37 | D: CSV (v0.21) | — | RFC-4180, parity, stable | **PASS** — report/attrib/compare/calibration/human/trend/roi `--csv` all render; refusals survive as rows ("insufficient data (n=1 < 5)"); attested/derived separated by `kind` column; method/match tags are columns; LF-only (0 CR chars); byte-identical across runs; `export --csv calls` = raw rows incl. `gap_ms`/`machine`, PII-clean. MCP `format: csv` **byte-identical** to CLI `report --csv` on the same ledger. | — | — |
| 38 | D: cleanup (v0.19) | — | allowlist only, views unchanged | **PASS** — dry-run lists (class · action · age · file · reason); `--apply` removed exactly the 3 planted allowlist items (2 aged pending buffers + 1 .tmp); ledger/policy/bin/limits/cursors survive `days = 0`; report byte-identical before/after; `CAGE_CLEANUP=0 cage import` leaves aged files, normal import auto-prunes. | — | — |
| 39 | D: exit codes + corrupt policy | — | 2/1/130 regimes | **PASS** — bad subcommand → 2; `--since garbage` → typed `error:` one-liner, exit 1; typed CageErrors never traceback (debug-gated traceback is the unexpected-exception boundary — verified in `cli.main`); accidental live corrupt-policy test (duplicate `[cleanup]` table) → `error: policy.toml: Cannot declare ('cleanup',) twice`, exit 1, capture unaffected; `cage watch` SIGINT → **130**. | — | — |
| 40 | D: doctor --bundle | — | redacted archive | **PASS** — 8 members (manifest/doctor/version/paths/footprint/policy-provenance/cursors); grep for canary + prompt text across members → clean. | — | — |
| 41 | D: MCP read | — | answer matches CLI | **PASS** — real MCP call (`cage_report`, `format: csv`) byte-identical to CLI against the same global ledger. (Testbed-side MCP ask via Copilot was offered in PAUSE #1; result not reported — covered by this protocol-level check.) | — | — |
| 42 | D: proxy | — | client rows land | **PASS** — stub OpenAI-compatible upstream + `cage proxy --port … --upstream …`; curl through it → response passthrough intact, exactly one call row `{agent: proxy, route: proxy, openai/gpt-5-mini, 1234/56}`. | — | — |
| 43 | D: `cage meter -- <cmd>` separator | — | documented usage works | **BUG → FIXED IN TREE** — argparse REMAINDER keeps the `--`; run() tried to exec `'--'` (FileNotFoundError, exit 1) while help documents `cage meter -- <cmd>`. Fixed in `cage/metercmd.py` (strip leading `--`, mirroring `graphifymeter.py:97`); regression test `tests/test_meter.py::test_metercmd_tolerates_dash_dash_separator` (also pins child-exit-code propagation 0/3/2). 12/12 meter+graphify tests pass; end-to-end `cage meter -- true` → 0, `-- false` → 1. | bug (fixed) | — |
| 44 | E: history seed + pre-task half | — | band, --record | **PASS** — 5 closed `docfix` tasks seeded via the public library (`record_call` with task ids — the documented consumer path, labelled LIBRARY-SEEDED); `compare --by label`: agent-only n=5 measured medians/IQR + honest no-delta refusal; `estimate --label docfix`: modeled band, "confidence: none self-reported"; `--record t-docfix-live` stamped `est_tokens/est_usd/est_n` **+ band bounds** onto the open task row (verified in tasks shard). Note: estimate history included a zero-actual closed task (t-manual-1) that calibration would skip — inclusion asymmetry, cosmetic. | — | — |
| 45 | E: PAUSE#2 session 1 (docfix live) | claude×cli | rows + provenance | **PASS (with note)** — session `5661bd01`: 11 unique rows live, 324,675 tok in. Agent edited README but **did not commit** (prompt asked for one) — no `hooked` provenance possible; the SessionEnd transcript-fallback row correctly recorded the README edit against HEAD (method `transcript`, conf 0.67). Loop unaffected (`cage outcome` snapshots HEAD). | — | — |
| 46 | **Live-capture duplicate rows (hook race)** | claude×cli+vscode | one row per turn | **BUG → FIXED IN TREE** — this machine has BOTH user-level (legacy, absolute-path) and project-level (shim) Claude hooks; every Stop/SessionEnd fires two `cage hook-*` processes; both loaded `seen` before either appended → **every live turn double-appended (same id twice)**; the read path doesn't dedupe → `report` double-counted those sessions (18 rows/9 ids, 30/16, 22/11; testbed July shard held 34 duplicate rows). One-shot imports were clean (single writer under `import.lock`). `lockutil`'s docstring names exactly this scenario but `hooks._capture_calls` never took the lock — implementation gap vs documented design. **Fix:** `_capture_calls` now wraps its read-check-append in `lockutil.locked(state/import.lock)` (fail-open, debug line on miss). **Regression test** `tests/test_transcript.py::test_capture_calls_serializes_across_processes` — deterministic cross-process interleave; verified FAILS pre-fix, PASSES post-fix. Full suite 571 passed. **Testbed ledger repaired** (34 duplicate rows dropped by id, first-occurrence-wins — test artifact only; global ledger verified clean). Field impact: anyone with old user-level + new project-level wiring double-counts live Claude spend until this ships. | **bug (fixed)** — was inflating live-captured spend 2× | yes (fix adds `hook.capture.lock` miss line) |
| 47 | C/E: resolver precedence (PAUSE#2 session 2) | claude×cli | no project .cage → global | **PASS** — `.cage` stashed; session `b5d273d1` (8 rows) landed **live in global** `~/.cage` via the user-level SessionEnd hook (resolver → global when no project ledger); **no stray `.cage/` grew in the testbed**; `cage doctor` names `project (.cage/)` in the testbed and `global (~/.cage)` from `$HOME`; `.cage` restored by mv-back (superset of the plan's `cage init`). Side observation (by design): session 2's SessionStart slug-scoped import backfilled the 3 earlier testbed sessions into global too — both ledgers now hold those rows; each ledger is an independent view (pull-based capture), noted, not a bug. | — | — |
| 48 | E: loop close + calibration | — | scored against recorded band | **PASS** — live session bound to `t-docfix-live` via two zero-token task-bound boundary calls (JOIN-SEED, labelled; adds 0 tokens to measured totals — exercises the session-window fallback contract); `outcome --label docfix --minutes 6` (attested receipt written); `calibration`: n=1, ratio 10.1739, in-band **false**, hit-rate 0% — the estimator's measured confidence honestly reports the miss (seeded history ≪ real session); CSV row matches text. `compare` group median moved correctly with n=6; `--agent-only` suppresses the total-cost line; `verdict graphify` updated live (redo-rate 0/6 measured). `cage query` traces estimate-band + compare-delta with live gate values (n=5). | — | — |
| 49 | F: fleet study end-to-end | — | enrollment→bundles→analyst | **PASS** — `study join baseline` → opaque random ids (`m_0240…`, `m_3b1e…`), doctor printed; phase markers land in `ledger/study.jsonl`; enrolled writes stamp `machine`, the 5,167 rows written pre-enrollment carry none (historical-ts rows swept in *after* enrollment correctly get stamped — write-time semantics); m2 given a deliberate plugin-phase gap. `export --study`: manifest = version + machine id + span + row counts + `refresh {ran, new_calls}` (m2's `--no-import` → "snapshot only"); **hostname/username grep over both bundles → NONE**. Analyst dir: import both bundles **twice** → 0 merged on rerun, report totals byte-identical. `study report`: coverage FIRST, m2 baseline "MISSING — no rows" (gap kills), 28,167 unphased rows excluded-and-said, paired delta **refusal** (n=1 < 5 machines with both phases — expected with 2 machines; numeric path is S9-tested), pooled machine-days `measured`, total-cost line, UNPRICED ⚠ resurfaces under the analyst's own (unaliased) policy. Same-day phase flips resolve at day granularity — the machine-day sample unit per plan §4.9, noted not a bug. | — | — |
| 50 | G: determinism | — | byte-identical double-runs | **PASS** — 25/25 Part-D commands byte-identical across back-to-back runs (incl. CSVs, verdict, study report, query); `CAGE_DEBUG=1` → derived output unchanged. | — | — |
| 51 | G: fail-open + adversarial | — | exit 0, never silent | **PASS** — fully read-only ledger (`chmod -R a-w`, files not just dir — dir-only doesn't block appends): `record_call` returns `''` without raising, debug.log gains `ledger.append · write-failed`; `cage import` exits 0. Truncated mid-row shard tail: report/attrib/budget/human/study-report all render; restored. Corrupt policy (syntax error): `error: policy.toml: Expected '='…`, **exit 1**; `cage import` under corrupt policy still exits 0 (capture fail-open). | — | yes (`ledger.append write-failed`) |
| 52 | G: $0/no-network | — | offline behavior identical | **PASS** — full 26-command sweep under macOS `sandbox-exec (deny network*)` (Wi-Fi-off equivalent for the processes; toggling real Wi-Fi would sever the driver session): all exit 0; 25/26 byte-identical to online runs; the one "diff" was `cage import` picking up 10 fresh driver-session rows between runs (timing, not network). | — | — |
| 53 | G: PII + no-scheduler | — | clean, nothing installed | **PASS** — canary + commit-message grep over `.cage/` and `~/.cage/` → clean; `launchctl` hits are Apple `*syncAgent` substring noise (no cage entries), `crontab -l` empty, no LaunchAgents. Scheduler hint printed by doctor, never installed. | — | — |
| 54 | G: portable wiring (v0.20) | — | greps + clone-sim + migration | **PASS** — all committed wired files free of absolute paths (documented `.kiro/settings/mcp.json` exception present, doctor advises gitignore); clone-sim (rsync sans `.git`/`.cage/state`) → doctor portability ✔, committed shim resolves cage 0.22.1; planted absolute entry in `.mcp.json` → doctor `·` names the file + teammate impact + remedy; `cage setup --claude` migrates it back to `${CLAUDE_PROJECT_DIR:-.}/.cage/bin/cage-run`; doctor clean again. | — | — |
| 55 | G: launcher mode (v0.22) | — | round-trip + env override | **PASS** — `setup --python-launcher` persists `[wiring] python_launcher=true`; flag-only run leaves shim standard and **doctor flags the policy↔shim drift**; `setup --all` fans out: sh shim `python3 -m cage` (0 exe-shaped refs), cmd shim `py -3`, copilot hook `python3 -m cage import…`, codex MCP `command="python3"`; shim proven end-to-end via a PATH whose python3 has cage installed; doctor honestly warns capture is off when the default PATH python lacks cage. Flip to `false` + re-setup → **all 9 wired files byte-identical to pre-launcher hashes**. `CAGE_RUN_PYTHON=1` honored by the standard shim. | — | — |
| 56 | G: zipapp (v0.22) | — | label + parity | **PASS** — local `tools.buildpyz` → `dist-pyz/cage.pyz`; `--version` = `cage 0.22.1 (zipapp)`; demo reproduces §4.4 exactly ($0.1242 / 41,400); `report` over the testbed ledger **byte-identical** to the installed cage. Local build only, nothing uploaded. | — | — |
| 57 | G: docs hygiene + versions | — | clean root, consistent versions | **PASS (with note)** — docs/ root = living docs + operations only; archive indexed; 9 "Built from:" CHANGELOG links; **no broken docs links** in README/CHANGELOG/docs indexes; CLAUDE.md carries the lifecycle rule; `cage --version` = `__version__` = CHANGELOG top = 0.22.1. Note: the two uncommitted working docs (`docs/cage-claude-code-prompt-full-test-run.md` untracked, plan modified) are this run's driver — index/commit or archive them with the run record. README/CLAUDE.md test counts updated 569 → **571** in-tree (two regression tests added by the fixes; release-time changelog still owed by the release that ships them). | — | — |
| 58 | Close: end-of-run gates | — | all green post-fixes | **PASS** — full suite **571 passed, 1 skipped**; dummyrepo **S1–S13 all PASS** (re-run post-fixes); skillgen `--check` clean. | — | — |

## Real-ledger reconciliation (`~/.cage/ledger/`)

| file | baseline | final | delta | explanation |
|---|---|---|---|---|
| calls-2026-02/04/05.jsonl | absent | 13 / 18 / 220 | +251 | first-ever full global all-agent sweep (the plan's sanctioned `cage import` from `$HOME`, resolver step) backfilled the user's real historical codex/copilot/claude logs into month shards |
| calls-2026-06.jsonl | 12,762 | 24,575 | +11,813 | same sanctioned full sweep (11,688 claude + 373 codex + 60 copilot + 2 kiro imported in that one run, distributed by row ts) |
| calls-2026-07.jsonl | 4,813 | 5,208 | +395 | this driver session's own hook imports (project=cage, the largest share), the sanctioned resolver-precedence session `b5d273d1` + slug backfill (44 testbed rows), and the user's other real sessions during the run (fux/prompts/kiro/copilot) |
| receipts-2026-06.jsonl | 7 | 7 | 0 | untouched |
| policy.toml | md5 d2ac5363… | identical | 0 | untouched |

**No fixture/planted/synthetic row entered the real ledger** — planted logs lived only in sandboxed scratch homes behind env overrides; all global growth is the user's genuine usage. Canary grep over `~/.cage` clean.

## Plan checklist state for this run

- **Part A** — all 5 boxes ✔ (rows 1–5)
- **Part B** — all 3 boxes ✔ (rows 7–9)
- **Part C** — matrix: claude×cli ✔, claude×vscode ✔, codex×cli **N/A (not run)**, codex×vscode ✔, copilot×cli ✔, copilot×vscode ✔, kiro×IDE ✔, kiro×cli **N/A**; all 6 trailing boxes ✔ (rows 10–24)
- **Part D** — all 19 bullets ✔ (rows 26–43; 2 plan-text drifts noted, 2 bugs found)
- **Part E** — all steps ✔ (rows 44–48; calibration honestly scored the miss)
- **Part F** — all 6 steps ✔ (row 49; delta refusal expected at n=2 machines)
- **Part G** — all 12 bullets ✔ (rows 50–57)

## Proposed plan edits (not applied mid-run per driver rules)

1. Part D `--since` bullet: "`--since <date>`" → "`--since <window>` (7d/24h/2w; cutoff respected across month shards)".
2. Part D/E `cage outcome <task> --ok` → `cage outcome <task> [--redo]` (success is the default; there is no `--ok` flag).
3. Part C export-sweep bullet: note that the manifest line for `--no-import` reads "snapshot only (no sweep)".
4. Part G offline bullet: allow `sandbox-exec (deny network*)` as the Wi-Fi-off equivalent when the run is driven by a network-attached agent.

## Findings disposition (final)

1. **#46 hook-race duplicate capture — FIXED IN TREE** (was: live Claude spend double-counted whenever user-level + project-level wiring coexist; fix = lock in `_capture_calls`; regression test proves fail-before/pass-after).
2. **#35 graphify double-metering — FIXED IN TREE** (wrapper-side detection + `CAGE_GRAPHIFY_METERED=1` forward handshake; option (b) chosen over (a) alone — needs no graphify release — and over (c) — no brittle setup-time version sniffing; end-to-end verified +1 receipt per wrapped query). Graphify-side follow-up suggested: respect the env var to restore `--task` binding under the wrapper.
3. **#43 `cage meter --` separator — FIXED IN TREE.**
4. **#27/#31 plan-text drifts — OPEN (docs)**: proposed edits above; apply to `docs/full-test-plan-sibling-repo.md` at next release.
5. **#27b paper-cuts — OPEN (backlog)**: `report --scope` empty-state message misleading; `report --team` silent about a missing team ref; `cage why` renders `model /copilot/auto` with a dangling slash when provider is empty; estimate history includes zero-actual tasks that calibration would skip (asymmetry).

Final gates after all three fixes: **574 passed, 1 skipped** · S1–S13 all PASS · skillgen `--check` clean. README/CLAUDE.md counts updated to 574; changelog entry owed by the release that ships these fixes.

