> **ARCHIVED 2026-08-01 — implemented for v0.38 (unreleased).** History, not spec.
> Executed as a single pass, all six items. Living spec: the restored
> [../restricted-environments.md](../../docs/restricted-environments.md), the `cage query
> graphify-shims` entry, and [ADR 0007](../../docs/adr/0007-graphify-twin-pair-hand-paired-not-templated.md).

---

# Claude Code prompt — close v0.38's honesty debts (GF-DEBT)

**Model: Sonnet.** Seven items, all documentation/explainer, each with an explicit target.
No architectural judgement required — except the ADR, whose *decisions are already made*
(you are recording them, not making them). Escalate to Opus only if restoring
`restricted-environments.md` reveals that the launcher-mode behaviour it documented has
actually changed in code.

**No paid LLM calls. Do these before v0.38.0 is committed** — same change, not a
follow-up. The cage tree otherwise stays uncommitted; no commits from you.

## Read first

- [graphify-honesty-debts.handoff.md](v0.38-graphify-honesty-debts.handoff.md) — all seven,
  with file/line targets
- `docs/shim-contract.md` (B1–B8, D1–D7) · `cage/data/shims/graphify{,.cmd}`
- `cage/explain_data.py` — copy the shape of `stale-wiring` / `kiro-routing`
- `docs/adr/TEMPLATE.md` and ADR 0006 (the worked example for a veto condition)

## The seven

**1. Restore `docs/restricted-environments.md`.** It existed (129 lines) and was deleted
in `b2c4253`. Recover it: `git show b2c4253^:docs/restricted-environments.md`.
**Restore, then update — do not rewrite**; the Tier 1/2/3 structure and the WDAC caveat
are hard-won. Verify all 8 citations resolve (`clicmds.py:438`, `doctorcmd.py:30,439`,
`paths.py:59`, `policy.py:260`, `runshim.py:30,144`, `CLAUDE.md:738`), and check the
launcher surface it describes still matches the code.

**2. State GF-LAUNCHER where users are.** Under `--python-launcher` no `cage` is on PATH,
so **neither twin meters** (B5). The README currently claims Windows graphify works and
recommends `--python-launcher` two sentences later, with no hint they cancel out. Fix in
the README Platforms line (one clause), fully in the restored doc, and have `cage doctor`
say it when **launcher mode is on and an interceptor is installed**.

**3. A `cage query` entry for the twin pair.** Doctor can now *fail* on a root carrying
only the twin this OS can't resolve — with no explainer to run. Cover: why two twins ·
PATHEXT has no extensionless entry · `.EXE` precedes `.CMD`, so resolution is
directory-major/extension-minor and `bin/` is safe by construction · the marker set as
identity · D1 (`call`, not `exec`) · the launcher gap. Interpolate live from
`paths.graphify_shims`; carry `code_refs` + `plan_ref` like every other concept entry.

**4. ADR 0007 — record three decisions you are not re-making.** Both twins install on
every OS · **hand-paired, not templated** · contract in `docs/`, not package data.
Veto condition, verbatim in substance:

> **Contingent:** templating becomes revisitable only when a **third** interceptor exists
> *and* at least two share a syntax family. **Invariant:** the written contract, not the
> code, is the shared artifact — that does not change with tool count.

**5. cage-lab states POSIX-only validation.** `03-verify.md`: a green lab run is **not**
Windows coverage. `01-setup.md`: the PATH-proof law proves the POSIX shim.

**6. The corpus-sizing rule.** The first CI corpus was too small, so every query returned
`unmeasurable` and the leg **passed while asserting nothing**. Write the rule down (in
`shim-contract.md` or the CI section) and — preferred — make an all-`unmeasurable` run
**fail loudly** instead of passing. If that turns out to be non-trivial, write the rule
and say why the check was deferred.

**7. Apply the proposal lifecycle.** CLAUDE.md now carries it (parked → picked up →
implemented → **archived**). `windows-graphify-interceptor.md` is already archived as
the worked example; once **ADR 0007** exists, add it to that archived proposal's header
as a living-spec anchor beside `shim-contract.md`.

## Hard constraints

- **Do not fix GF-LAUNCHER.** This documents it; the fix is a decision that must move
  both twins together. It stays open in `OPEN-WORK.md`.
- **Do not template the twins** — see the ADR you are writing.
- Restore-then-update, never rewrite-from-memory, for item 1.
- Explainer entries interpolate live values; no hard-coded paths (cage law).
- stdlib only; don't touch `work/regression/**`; no commits.

## Acceptance criteria

- [ ] All 8 `restricted-environments.md` citations resolve; content current, not invented
- [ ] GF-LAUNCHER stated in README + the restored doc + doctor's launcher∧interceptor case
- [ ] `cage query <new-id>` explains the twin pair; live-interpolated
- [ ] ADR 0007 exists with the templating veto condition
- [ ] cage-lab says POSIX-twin-only in `03-verify.md` and `01-setup.md`
- [ ] Corpus-sizing rule written; vacuous-run detection built or explicitly deferred
- [ ] Archived WIN-GF proposal's header names ADR 0007; no other proposal is silently built
- [ ] `just test` green (979 + any new); re-blessed goldens named
- [ ] DOC-REGISTRY rows for the restored doc and the ADR; IMPLEMENTATION / WORKLOG /
      OPEN-WORK updated; pair archived to `work/archive/v0.38-graphify-honesty-debts.*`

## Handback

Whether the restored doc needed correcting against current code, and what · the doctor
line for launcher∧interceptor (paste it) · the `cage query` id you chose · whether the
vacuous-CI-run check was built or deferred and why · re-blessed goldens.
