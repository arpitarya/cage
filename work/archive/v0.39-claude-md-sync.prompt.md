> **ARCHIVED — executed for v0.39 (unreleased), 2026-08-01.** Both tasks done as
> specced: proposal 1 applied verbatim, proposal 2 declined (independently
> re-verified, same verdict). Zero code changes; suite green. **History, never
> current spec.**

# Claude Code prompt — CLAUDE.md sync (CMD-SYNC)

**Model: Sonnet.** Docs-only, one proposal to apply and one to decline, both decided.
Escalate to Opus **only** if you verify the code and reach a different verdict on
proposal 2 than this handoff — in which case stop and report rather than proceeding
either way.

**No paid LLM calls. No code changes. The cage tree stays uncommitted.**

## Read first

- [claude-md-sync.handoff.md](v0.39-claude-md-sync.handoff.md) — the verdicts and the evidence
- `proposals/claude-md-prices-file.md` (**apply**) ·
  `proposals/claude-md-sources-authority.md` (**decline**)
- `cage/paths.py` `resolve_log_sources` — read the **whole docstring**; it decides
  proposal 2

## Task 1 — apply proposal 1 (`prices.toml`)

**Verified true:** `data/prices.toml` exists, `policy._BUNDLED_PRICES` loads it, and
**CLAUDE.md mentions it zero times.** The flow diagram still reads
`cage.toml (prices/order/budgets)`, naming the wrong home for the vendor rate card in
the file every agent loads.

Apply the proposal's edits — flow diagram, caption, Must-Know bullets — so `cage.toml`
(order / budgets / routing) and **`prices.toml`** (model prices, `[credits]`) read as the
two config inputs. Keep its governing sentence verbatim: **vendor facts move, routing
decisions stay.**

## Task 2 — decline proposal 2 (`[sources]` authority)

**Verify this yourself before acting.** Read `resolve_log_sources`'s docstring. It ends:

> Precedence per built-in agent: **env override > policy `[sources]` > built-in** …
> **Fully additive: an empty/absent `[sources]` returns exactly the built-in registry.**

That is what CLAUDE.md already says. The proposal asks for the opposite ("`[sources]` is
the ONLY source of log paths, no built-in runtime fallback, captures nothing loudly") —
a **Directive A end-state that never fully shipped**. The `sources_seed` /
`sources_drift` machinery exists; the runtime fallback was never removed.

**Applying it would replace a true statement with a false one.** So:

- **Do not apply it.**
- Archive it as **declined**, per the proposal lifecycle: header names the decision, the
  date, the decider (Arpit, via this handoff), and cites
  `paths.resolve_log_sources`'s docstring as the contradicting evidence.
- **If your reading of the code differs — stop and report.** Do not apply a partial
  version to split the difference.

## Hard constraints

- **Zero code changes.** If applying proposal 1 seems to require one, you have
  misread it — stop.
- **CLAUDE.md was edited three times today** (CODEX-OUT, DEBT, the doc-citation sweep).
  **Re-read it before editing**; never work from a remembered copy.
- Don't touch `work/regression/**`; no commits.

## Acceptance criteria

- [ ] `grep -c "prices.toml" CLAUDE.md` > 0 (zero today); diagram + caption correct
- [ ] "vendor facts move, routing decisions stay" present
- [ ] Proposal 2 **not applied**; archived as declined with the evidence cited
- [ ] Proposal 1 archived as implemented per the lifecycle
- [ ] `git diff --stat cage/` empty — no code touched
- [ ] `just test` green
- [ ] IMPLEMENTATION / WORKLOG / DOC-REGISTRY / OPEN-WORK updated; **CMD-SYNC removed**
      from the queue

## Handback

The CLAUDE.md lines you changed · **your independent verdict on proposal 2 and the
evidence you used** (agreeing is fine; say so explicitly) · confirmation no code was
touched.
