> **ARCHIVED 2026-08-01 — implemented for v0.39 (unreleased).** History, not spec.
> Executed as written; see the archived
> [handoff](v0.39-codex-purge.handoff.md) header for the outcome and the one
> correction. Living spec: the CHANGELOG `Removed` entry.

# Claude Code prompt — purge Codex (CODEX-OUT)

**Model: Opus.** This is a deletion with a live look-alike in the blast radius — the
class CLAUDE.md's tier rubric names explicitly. The typing is trivial; **telling the
three Codexes apart is the whole job**, and getting it wrong silently UNPRICES real
Copilot traffic.

**No paid LLM calls. The cage tree stays uncommitted** (commits in `cage-lab` only).

## Read first

- [codex-purge.handoff.md](v0.39-codex-purge.handoff.md) — the three-Codexes table, the
  accepted trade-off, the per-file scope
- `CLAUDE.md` — wiring-liveness (why the scan existed), the removed-verb rule
- `cage/data/prices.toml` around line 176 — **read the comment before touching anything**

## The one thing that must not go wrong

**Three unrelated things are spelled "codex".** Only the first is being removed:

1. **The agent** — `paths.codex_home()`, `wiringscan`'s `~/.codex/config.toml` scan,
   `doctorcmd`'s `.codex/hooks.json`, `CODEX_HOME` in `doctorbundle`'s env allowlist,
   and their tests. **DELETE.**
2. **The model family** — `gpt-5.3-codex`, `gpt-5.2-codex`, … in `data/prices.toml`,
   plus `policy.py`'s `…-codex-high` → `…-codex` effort fold. **These are OpenAI model
   ids that Copilot emits.** The file says so: *"OpenAI — Codex CLI + Copilot (both emit
   gpt-family ids)"*. **KEEP. Untouchable.** Deleting them silently unprices a supported
   agent.
3. **Stale prose enumerations** — "Claude today; Copilot/Kiro/Codex leave it empty" in
   `schema.py`, `ledger.py`, `report.py`, `proxy.py`, `runshim.py`, `paths.py:14`.
   **Drop the word, keep the sentence.**

`paths.py:106,122,126` are a judgement call: they describe **verb-tail parsing** of
legacy commands like `cage import --agent codex`. Decide per site whether each still
serves a migration path, and **state which you kept and why**.

## Approach

1. **Classify before deleting.** Produce the category-1/2/3 list for every hit in
   `cage/` and `tests/`, and have it checked against the handoff's table. Anything you
   can't classify confidently: stop and ask.
2. Delete category 1, file by file. `tests/test_wiringscan.py` is the biggest site
   (~32 refs) — **delete codex cases; never soften a surviving assertion** to make the
   file cohere after removal.
3. Edit category 3 prose.
4. **Prove category 2 untouched:** `git diff --stat cage/data/prices.toml` must be empty,
   and a Copilot call on a `gpt-5.x-codex` id must still price. Assert it.
5. Re-bless goldens (doctor/wiring output will likely move) and name each.

## Hard constraints

- **`data/prices.toml` byte-identical.** Non-negotiable.
- **History is never rewritten:** `CHANGELOG.md`, `work/regression/**` (append-only),
  `IMPLEMENTATION.md`, `WORKLOG.md`, `work/archive/**`, PLAN.md's historical passages.
- **Do not split `paths.py`** — the DEBT proposal's fix-on-contact rule applies to its
  *named* seams; agent-homes isn't one yet. Instead **add `agenthomes` to that
  proposal's seam list** so the next touch does a clean move.
- No test weakened to accommodate the deletion.
- stdlib only; fail-open write paths stay fail-open; no commits in `cage`.

## The trade-off you are implementing (state it, don't bury it)

Removing `wiringscan`'s codex scan means a machine wired before v0.33 keeps a **dead
`cage` verb forever, undetected** — the user-level F1 class. Arpit accepted this: Codex
has been unsupported for five releases. **The CHANGELOG `Removed` entry must name the
lost detection**, so the trade is visible rather than silent.

## Acceptance criteria

- [ ] `grep -riI codex cage/` returns only category 2 (model ids + the effort fold)
- [ ] `git diff cage/data/prices.toml` empty; a `gpt-5.x-codex` Copilot call still prices — tested
- [ ] `CODEX_HOME` gone from code and the doctor-bundle env allowlist
- [ ] Codex test cases deleted; no surviving assertion relaxed
- [ ] `just test` green; re-blessed goldens named; test count refreshed in README + CLAUDE.md
- [ ] CHANGELOG `Removed` entry naming the lost pre-v0.33 stale-verb detection
- [ ] `proposals/structural-debt.md` lists `agenthomes` as a seam
- [ ] Pair archived to `work/archive/vX.Y-codex-purge.*`; IMPLEMENTATION / WORKLOG /
      DOC-REGISTRY / OPEN-WORK updated

## Handback

Your category-1/2/3 classification (and anything the handoff mis-filed) · the
`paths.py:106/122/126` decisions and why · proof `prices.toml` is untouched and codex
model ids still price · re-blessed goldens · final test count.
