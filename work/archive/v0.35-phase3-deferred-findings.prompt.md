# Claude Code prompt — Phase 3: deferred low-priority findings (F3, F5, F7) → v0.34.0

**Model: Sonnet.** Well-specified, mechanical, verifiable against the 2026-07-22
report's evidence. No design fork. The only soft spot — F3's Kiro-path
confirmation — needs a real install to check, not judgment; flag it, don't escalate.

---

You are closing the three low-priority findings the capture report parked. Full
spec: `docs/phase3-deferred-findings.handoff.md` — read it first; its Definition of
Done and Non-negotiables are binding, and its **out-of-scope list is the point** —
each fix is small and additive.

## Context to load first

1. `docs/phase3-deferred-findings.handoff.md` — the spec.
2. `work/regression/2026-07-22-capture-report.md` §F3/§F5/§F7 — exact evidence + proposed fix.
3. `CLAUDE.md` — non-negotiables + the mechanical test-count rule.
4. Code: `cage/doctor.py`, `cage/report.py` (`--usd`), `cage/transcript.py::parse`,
   `cage/importcmd.py`.

## Required workflow

1. **Explore** each target before editing — confirm the current behaviour matches
   the report's evidence.
2. **Plan** the three changes + the files each touches; **pause for confirmation**.
3. **Implement each additively**, one at a time, tests alongside:
   - **F3** — `doctor` distinguishes "Kiro found but ~0 tokens" from healthy, and
     recommends the proxy (`cage proxy` / `cage meter`). Import logs Kiro `src`:
     existed?, bytes, rows parsed, tokens summed (counts/paths only — PII guard).
   - **F5** — add one cached-vs-fresh line/column to `report --usd`, using the real
     0.1× cache multiplier, not a hardcoded fraction. Change nothing else in `report`.
   - **F7** — verify `transcript.parse` stamps `gap_ms` across the whole transcript;
     log the skip where there's no prior-turn timestamp (don't fabricate). Report
     before/after coverage on a real transcript.
4. **Verify:** run the suite; refresh the test-count line (CLAUDE.md rule). Mark
   F3/F5/F7 resolved in the capture report with the shipping version.
5. **Release hygiene:** CHANGELOG + README "What's new" (replace) + `__version__`
   → v0.34.0. Archive this handoff/prompt pair on the release; link from CHANGELOG.
   **Do not publish from the laptop — GitHub release is the sole trigger.**

## Constraints (hard)

- `$0`/stdlib-only; deterministic; fail-open with swallows logged under `CAGE_DEBUG`.
- **Counts-never-content PII guard** — the F3 import log carries sizes/paths/counts,
  never token content.
- **Do NOT:** rewrite Kiro capture, restructure `report`, invent `gap_ms` where the
  source lacks a prior timestamp, or touch the capture hooks / `capture.log` (that's
  Phase 4 / the F6-fix — out of scope, see the field-gate note).
- CLAUDE.md steering edits: propose in the PR body, don't silently rewrite (except
  the test-count line).

## Acceptance criteria (self-check)

- [ ] F3: doctor state + proxy recommendation + import-time Kiro `src` logging
- [ ] F5: one cache-split line in `report --usd`, real multiplier
- [ ] F7: `gap_ms` coverage verified/extended; legitimate skips logged, none fabricated
- [ ] Tests per fix; test-count line refreshed; report's F3/F5/F7 marked resolved
- [ ] v0.34.0 CHANGELOG + README "What's new"; handoff/prompt archived
- [ ] Derived views still byte-identical

## Guardrails

- **Ask before:** publishing, restructuring any `report` view, or shipping F3
  against an unverified Kiro CLI path (Open Q2 — confirm the path or note it unverified).
- **If the Kiro CLI path can't be confirmed** (no install), ship F3 against the
  VS-Code `globalStorage` path with a `doctor` note that the CLI path is unverified —
  do not guess a path.
- If a requirement is ambiguous or conflicts with the code, STOP and ask.

## Not in scope (for the record)

- The **F6 breadcrumb-to-hook-path fix** and **Phase 4 hook deletion** are coupled
  and gated — see `work/regression/2026-07-24-field-gate-post-heal-check.md`.
