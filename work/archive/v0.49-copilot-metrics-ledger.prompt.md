---
doc: prompt — copilot-metrics: `.cage/ledger/copilot/`, the per-chat Copilot metrics ledger
status: ARCHIVED — implemented v0.49 (unreleased) on 2026-08-14
pair: [v0.49-copilot-metrics-ledger.handoff.md](v0.49-copilot-metrics-ledger.handoff.md)
---

> **ARCHIVED — implemented for v0.49 (unreleased) on 2026-08-14.** History, never
> current spec.

# Claude Code prompt: copilot-metrics — the `.cage/ledger/copilot/` metrics ledger

**Model:** Sonnet — additive, fully-specced capture work; every decision is locked in
the handoff and the change map names every insertion point. Escalate to Opus only if a
change-map fact turns out stale (that makes it diagnosis).
**Progress:** 100% — P0–P5 built and green (2026-08-14).

You are building a new ledger kind: `.cage/ledger/copilot/chats-YYYY-MM.jsonl` —
per-chat Copilot metrics (tokens in/out · cached · per-request credits · session
credits · nano-AIU · timing) captured verbatim from **all five** on-disk stores. The
full spec is `docs/copilot-metrics-ledger.handoff.md` — read it first; its
**Definition of done** and **Non-negotiables** are binding. Where this prompt and the
handoff disagree, the handoff wins. Store facts (which file, which prop) are grounded
in `work/research/2026-08-13-copilot-per-chat-usage-fetch-spec.md` — do not re-derive
them.

The handoff's **§4 change map** was researched 2026-08-13 against HEAD and names every
file, function, and line anchor — follow it and the build is a zap. Re-verify a mapped
fact only if the code in front of you disagrees; then STOP and report.

## Context to load first

1. `CLAUDE.md`, then `work/OPEN-WORK.md` (triage — COPILOT-METRICS is the item).
2. `docs/copilot-metrics-ledger.handoff.md` — the spec.
3. The precedents you will mirror (import, never reinvent):
   `cage/importcmd.py::_ingest_credits` · `cage/paths.py::savings_shard`/`shard` ·
   `cage/ledger.py::credits` (collapse read) · `cage/transcript.py::
   parse_copilot_vscode_calls`/`parse_copilot_calls`/`parse_kiro_cli_credits`
   (SQLite key-whitelist discipline) · `cage/schema.py::make_credit`/`make_call`
   (None-sentinel `credits`).
4. Contracts: `tests/test_floor.py` + `tests/test_output_spec.py` goldens (must pass
   untouched) · `tests/test_debug_coverage.py` (every new swallow site logs under
   `CAGE_DEBUG`) · `tests/test_queue_honesty.py`.

## Build order (phases — suite green after each)

- **P0 substrate**: `schema.COPILOT_METRIC_SOURCES` + `make_copilot_metric` ·
  `paths.Footprint.copilot_dir/copilot_shard/copilot_shards` + `shard()` routing ·
  `ledger.copilot_metrics_raw` + `copilot_metrics` (collapse per
  `(source, session, surface, request, call)`, winner max
  `(tokens_in+tokens_out, credits or -1, id)`).
- **P1 always-on parsers**: extract `transcript._vscode_chat_requests` (pure refactor —
  existing vscode fixture pins calls output byte-identical), then
  `parse_copilot_vscode_metrics` + `parse_copilot_cli_metrics`
  (cumulative-verbatim, float credits, never `int()`).
- **P2 wiring**: `importcmd._is_chat_session_file` (fixes dispatch for the new roots)
  · `_ingest_copilot_metrics` · call sites in `import_copilot` (reuse the per-source
  `files` list; no rescan).
- **P3 new chat roots**: two `_builtin_sources` tuples (`emptyWindowChatSessions`,
  `transferredChatSessions`); `no-workspace/` is already covered — do not add it.
- **P4 gated stores**: `paths.copilot_metric_sources()` +
  `parse_copilot_sidecar_metrics` / `parse_copilot_debuglog_metrics` (whitelist:
  model/tokens/ttft/ts/spanId ONLY — the same lines carry prompt bodies) /
  `parse_copilot_otel_metrics` (sqlite `mode=ro`, denormalized `spans` columns ONLY,
  never `span_attributes`/`span_events`) + the gated-store loop in `import_copilot`.
- **P5 surface + docs**: doctor advisory `copilot-metrics` line · `explain_data`
  entry `copilot-metrics` · every §9.5 doc box (incl. `docs/copilot-capture.md`
  — the standing copilot-numbers one-pager fires on this change).

## Constraints (hard)

- Counts-never-content — whitelist reads on debuglog + otel; a fixture `userRequest`
  string must be asserted absent from the written shard bytes. No chat titles.
- Absence ≠ zero: `credits`/`session_credits`/`nano_aiu` are None-sentinel; never sum
  session/cumulative rows; never derive credits from nano-AIU or tokens.
- Capture-only: NO derived view reads the kind; `report` + `insights chats` stdout
  byte-identical with the `copilot/` tree present vs deleted (write this test).
- Fail-open per file/db, `CAGE_DEBUG`-logged. $0 stdlib (`sqlite3` is stdlib).
  Append-only: ids fold values; never rewrite a shard. No cleanup class for the dir.
- Do NOT: touch `creditprice`/pricing · edit the verified fixture corpus under
  `tests/fixtures/transcripts/` · add the metrics stores to `agent_log_sources` ·
  relax `test_floor`/golden assertions · fix the v0.44 calls-parser credit-delta bug
  here (separate item).

## Required workflow

1. **Triage** `work/OPEN-WORK.md`; verify the COPILOT-METRICS line exists.
2. **Explore** the §4-mapped code; on any stale fact, STOP and report before coding.
3. **Implement by phase**, `just test` green each time; append
   `work/IMPLEMENTATION.md` per phase.
4. **Docs in the same change** — every handoff §9.5 box; `CLAUDE.md` edits are
   **proposed and flagged for Arpit**, never silently applied.
5. **Archive on implement** — green ⇒ move the pair to
   `work/archive/vX.Y-copilot-metrics-ledger.*` ("implemented for vX.Y (unreleased)"
   if untagged), update `docs/README.md` + `work/archive/README.md`, set this
   Progress line to 100% in the move, delete the OPEN-WORK line, add the two
   scope-out lines.
6. **Verify**: full suite green with NO golden re-bless (if a golden diffs, you broke
   capture-only — STOP) · `cage import --agent copilot` twice on a scratch ledger →
   second run appends 0 · `work/WORKLOG.md` entry with `Cost:` from `cage report`
   (or `Cost: unmeasured — <why>`).

## Acceptance criteria (self-check)

- [ ] Every Definition-of-done box in handoff §2 true.
- [ ] `tests/test_copilot_metrics.py` covers the handoff §9 list, including the
      content-whitelist assertion and the byte-identity test.
- [ ] Re-import idempotent; growth appends + collapse wins.
- [ ] All §9.5 docs done; pair archived; OPEN-WORK truthful.

## Guardrails

- STOP and ask if: a §4 fact is stale · a golden or `test_floor` diffs · the OTel db
  schema surprises you beyond the fail-open path · you feel the need to read a
  non-whitelisted field "just to key rows" (that is the design telling you no).
- Never delete ledger data; never rewrite append-only files.
