> **ARCHIVED — implemented for v0.47.0 (2026-08-07).** History, not spec. The
> living spec is CLAUDE.md's adapters section, `cage query graphify-coverage`,
> [ADR 0009](../../docs/adr/0009-kiro-cli-tool-run-bodies-read-transiently-never-persisted.md)
> and [docs/CLI.md](../../docs/CLI.md). Where this pair disagrees with them, they win —
> and it does disagree: **its central premise ("F2: chatSessions carries no tool
> result") was measured FALSE at the P0 gate**, which is the valuable part. The
> evidence that corrected it:
> [2026-08-07-graphify-store-evidence.md](../research/2026-08-07-graphify-store-evidence.md).

# Claude Code prompt: GFX-COV — graphify savings coverage for copilot-VSCode + kiro

**Model:** Opus — P0 is store-format diagnosis on real data with a blocking gate, P2
amends a counts-never-content law (ADR), and a wrong call fabricates savings numbers;
this is diagnosis-is-the-work territory, not an additive well-specced feature.
**Progress:** 100% — P0·P1·P2·P3·P4 all built and green (v0.47.0, 2026-08-07).

You are implementing GFX-COV: make cage's graphify savings ledger file receipts from
copilot **VS Code** and **kiro**, plus a cursor-blind backfill — with automated tests
for every route. The full spec is
[graphify-agent-coverage.handoff.md](graphify-agent-coverage.handoff.md) — read it
first; its Definition of Done and Non-negotiables are binding.

## Context to load first

- `CLAUDE.md`, then `docs/INTERVIEW.md` (the standing rules and scar tissue bind you).
- The handoff (same directory as this prompt).
- Code: `cage/graphifytx.py` · `cage/importcmd.py` (`_detect_graphify*`,
  `import_kiro`, `_kiro_leg`, `_scan`) · `cage/graphifymeter.py` ·
  `cage/transcript.py` (chatSessions shapes; the kiro-CLI whitelist comment) ·
  `tests/test_graphify_copilot.py` / `test_graphify_transcript.py` (patterns to extend).
- Evidence: `docs/WORKLOG.md` 2026-08-07 entry · `docs/adr/0005-*` (deferral law) ·
  `work/regression/2026-07-29-run-report.md` (copilot-CLI route validated on real
  traffic — it must not regress).

## Task

Five phases, strictly in order. **P0 is a blocking gate with a STOP.**

1. **P0 — field probes (STOP at the end).** On this machine, read-only:
   (a) have one real terminal graphify query run in a VS Code copilot chat (ask Arpit
   to do the run if you cannot trigger it), then inspect the matching
   `~/Library/Application Support/Code/User/workspaceStorage/<hash>/chatSessions/<id>.jsonl`
   — pin exactly which response-part keys persist the command and the result text,
   and every truncation/elision marker; (b) confirm cwd resolution via the sibling
   `workspace.json`; (c) run one graphify query through kiro-cli, then dump that
   conversation's `conversations_v2.value` (SQLite `mode=ro&immutable=1`, copy the
   DB first) — pin where commands and results live in `history[]`; (d) survey kiro
   IDE globalStorage for any command-bearing store. Write
   `work/research/<date>-graphify-store-evidence.md` (exact keys, versions, paths)
   and redacted fixtures under `tests/fixtures/transcripts/`. **Then STOP and
   present: per route, buildable or not, with the evidence.** A route without
   evidence is not built — it gets the P3 loud-gap treatment. Get Arpit's verdict on
   the handoff's OPEN QUESTIONS 1–2 before proceeding.
2. **P1 — copilot VS Code route.** New `detect_and_file_copilot_vscode` in
   `graphifytx.py`; remove the `chatSessions` skip in `_detect_graphify_copilot` and
   dispatch by store. Report-read receipts unconditionally (actual from disk); query
   receipts only per P0 evidence, with the truncation guard (marker ⇒ skip ⇒ debug
   event). Everything files through `_file_query`/`_file_report_read` — never a
   forked formula.
3. **P2 — kiro CLI route.** Detect graphify runs in `history[]`, reading bodies
   **transiently** (hashes/counts persist, never content). Author the ADR from
   `docs/adr/TEMPLATE.md` for this carve-out in the SAME change, and update the
   whitelist comment in `transcript.py` to name it. Receipts land in the sweep's own
   sink (respect `kiro_cli_workspace` scoping / `_kiro_leg` — never add a resolver).
4. **P3 — backfill + loud gaps.** `cage import --rescan-graphify`: full match set,
   detection only, idempotent. Doctor line + explainer entry naming which
   surfaces file and why the rest structurally can't.
5. **P4 — tests** (see below) then the full docs pass (handoff §9.5).

## Required workflow

1. **Explore before writing** — verify every handoff claim against HEAD; the repo's
   own rule is that doc markers are assertions, not evidence.
2. **Plan per phase** — before P1, lay out files-to-change and pause for
   confirmation; P0 ends in a mandatory STOP regardless.
3. **Implement incrementally** — keep `just test` green at every checkpoint; append
   the `docs/IMPLEMENTATION.md` entry at each green milestone (the standing rule).
4. **Update docs to match** — CHANGELOG + README "What's new", `docs/CLI.md` (new
   flag), explainer registry, FORMULAS only if a constant changed, OPEN-WORK
   (GF-AGENT-FIELD superseded; carry residuals), WORKLOG, DOC-REGISTRY rows, archive
   this pair on implement. For **CLAUDE.md**: the copilot-VSCode-skipped and
   kiro-token-log sentences go stale — **propose** those edits and surface them for
   Arpit's review; do NOT rewrite steering files silently.
5. **Verify** — `just test` (full suite), plus the new tests below. Do not report
   done until green and the docs pass is complete.

## Constraints (hard)

- stdlib only; fail-open on every write/detection path (debug-logged, never raised);
  deterministic ids via `receipt_id` only; everything `modeled`, never `measured`.
- A parse miss, truncated result, or unresolvable counterfactual files **nothing** —
  never a partial or fabricated saving.
- Counts-never-content: no command/answer byte persists anywhere in the ledger.
- Do NOT touch: shim twins, `pathshim._INTERCEPTOR`, `shim-contract.md`,
  pricing/`receiptprice`, `schema.py` (a needed schema change = STOP and ask),
  existing receipt ids (byte-stable), the floor test's assertions.
- claude and copilot-CLI routes must not regress (their tests + the 2026-07-29
  validated behavior are the baseline).

## Acceptance criteria (self-check before finishing)

- [ ] chatSessions fixture → report-read receipt files; query receipt files per P0
  evidence; truncated fixture files nothing.
- [ ] kiro-CLI fixture → query receipt files into the correct sink; ADR authored.
- [ ] Cross-route dedupe: shim-first ⇒ store route defers — tested per new route.
- [ ] `--rescan-graphify` backfills a cursor-consumed session; idempotent; in CLI.md.
- [ ] Unbuildable stores named in doctor + explainer, never silent.
- [ ] PII grep test: zero fixture content bytes in any ledger file.
- [ ] Full suite green; goldens re-blessed only where output legitimately changed.
- [ ] Docs pass complete (handoff §9.5), CLAUDE.md edits proposed not applied,
  pair archived on implement.

## Tests

Add/extend: `tests/test_graphify_vscode.py`, `tests/test_graphify_kiro.py`,
`tests/test_graphify_copilot.py` (rescan + regression), fixtures under
`tests/fixtures/transcripts/` (real-shape, redacted; `UNVERIFIED-FORMAT` label where
P0 could not confirm). Cover: detection correctness, false-positive guards,
E2E import + idempotency, cross-route deferral, truncation guard, PII grep,
determinism (two runs ⇒ identical rows). Run: `python -m pytest -q`.

## Guardrails

- STOP and ask rather than guess when: P0 evidence contradicts the handoff, a schema
  or substrate change looks necessary, a store must be written to, an id scheme
  would change, or "make it work" would require fabricating a counterfactual.
- Never run destructive commands against real agent stores; probe copies, read-only.
- If a requirement here conflicts with CLAUDE.md or PLAN.md, those win — say so.
