# Claude Code prompt: `[sources]` in policy (plan Phase 4)

You are implementing configurable import paths. Spec:
**`docs/policy-sources.handoff.md`** (binding) → plan Phase 4 block
(rationale). **Sequencing gate:** `policy-sync` must be in the tree; confirm
where `cli-tiering` stands and order hint strings accordingly — STOP and ask
if the queue state is unclear. **No commits, tags, pushes, publishes.**

## Context to load first

Handoff §4 list: `paths.py` (`agent_log_sources` — the ONE resolution point),
`importcmd.py` (sweep, cursors, per-agent import fns), `pathprobe.py`,
`policy.py`, `doctorcmd.py` (portability check), `policysync.py`,
`explain_data.py`, `tools/dummyrepo/run.py`, `CLAUDE.md`.

## Task, in order

1. **Explore** the current resolution + probe + cursor flow end-to-end;
   confirm cursors key on resolved absolute paths.
2. **Plan** — the tagged-candidates return shape, reserved-agent-key
   validation, where the sources doc section lives. Pause for my
   confirmation.
3. **Implement:**
   - `[sources.<agent>] paths` (additive; `replace = true` per table) +
     custom tools with mandatory `format` (reserved keys: the four agent
     names); `~`/env expansion; glob entries rejected with a typed error.
   - `agent_log_sources(pol)` returns provenance-tagged candidates; import
     sweep and `doctor --paths` both consume the tags (no second resolver).
   - Custom-tool rows stamp `agent=<name>`; existing parsers by declared
     format; cursors/dedupe/fail-open identical to built-ins; export sweep
     and `CAGE_CAPTURE` honor them.
   - Doctor: provenance column (`built-in|policy|env`), overlap flag,
     committed-absolute-path portability warning (`~` and global policy
     exempt), `disabled by policy` label for replace+empty.
   - `policy sync` ownership: `[sources]` never touched (bundle ships none —
     asserted).
4. **Docs/query:** `cage query sources` (live values); sources section +
   docs/README.md index; README capture one-liner; CHANGELOG in-tree;
   CLAUDE.md proposed-not-applied; skillgen regen if CLI text changed.
5. **Verify:** the two must-never-skip tests — empty-`[sources]`
   byte-identity and the full precedence matrix — plus expansion, custom-tool
   end-to-end (fixture log at a policy path → rows, reports split by agent
   name), cursor incrementality, warn/no-warn goldens, sync ownership,
   dummyrepo step, `just test` green.

## Constraints (hard)

- One resolution point; capture-side only (derived views untouched —
  determinism law).
- Fail-open sweep: a bad path is a debug-logged skip, never an error.
- Additive: no `[sources]` ⇒ byte-identical behavior everywhere.
- PII rules unchanged; local filesystem only; $0/stdlib; no commits.
- Handoff §10 default stands (disabled-by-policy stays visible in doctor).

## Acceptance criteria (self-check)

- [ ] Every handoff §2 box; both must-never-skip tests present and green.
- [ ] `doctor --paths` shows provenance for every candidate incl. custom
      tools; portability warning fires only where specced.
- [ ] Custom-tool import end-to-end green with correct agent stamping.
- [ ] Tree review-ready; zero commits.
