# Claude Code prompt: `cage policy sync` / `policy diff`

You are implementing the project-policy upgrade command. The spec is
**`docs/policy-sync.handoff.md`** — read it first; Definition of Done and
Non-negotiables are binding. **Sequencing gate:** if the `prices-route-tool`
or `pricing-freshness` pairs are still unimplemented in this tree, STOP and
ask — this work delegates to machinery they touch and rewords a hint one of
them introduces. **No commits, tags, pushes, publishes.**

## Context to load first

Handoff §4 list: `pricescmd.py` + `pricestoml.py` (the customized-detection
and managed-write machinery to REUSE — mirror how prices sync reconstructs
old bundled values; follow its mechanism exactly, don't invent a parallel
one), `policy.py`, `cfgio.py`, `initcmd.py`, `doctorcmd.py`,
`data/policy.toml`, `explain_data.py`, `tools/dummyrepo/run.py`, `CLAUDE.md`.

## Task

1. **Explore** how prices sync detects customization and applies writes;
   inventory every section/key the bundle ships vs what a v0.16-era project
   policy would have (git history of `data/policy.toml` is your inventory
   source).
2. **Plan** (module layout, the versioned-defaults mechanism you found, CLI
   group registration) — pause for my confirmation.
3. **Implement** per handoff §5:
   - `cage policy sync` (dry-run categorized diff: add / update / keep-
     customized / orphan, with counts) · `--apply` (idempotent, comment-
     preserving, temp-write + atomic replace) · `--yes` for the
     pre-`policy_version` per-row fallback · `cage policy diff` alias.
   - `[meta] policy_version` stamped by init and by every apply; init prints
     the one-time pointer (handoff §10 defaults accepted).
   - Pricing-family tables delegate to prices sync's implementation; its
     summary embeds in the output.
   - Doctor/freshness hint text: non-price drift → `cage policy sync`;
     pure price drift keeps `cage prices sync`.
   - `cage query policy-sync` entry (categories + neutrality invariant,
     live values).
4. **Verify:** every handoff §2 test — above all the two safety tests:
   **behavior-neutrality** (zero-customization project: apply then every
   derived view byte-identical) and **idempotent apply** (second apply is a
   byte-identical no-op). Plus category goldens, customized-never-touched,
   orphan-never-deleted, global-ledger target, exotic-TOML refusal path,
   dummyrepo scenario. `just test` green; skillgen `--check` clean.
5. **Docs:** CHANGELOG in-tree, plan-doc §3 note, pricing.md cross-link,
   README one-liner, CLAUDE.md proposed-not-applied.

## Constraints (hard)

- Never auto-applied by anything; dry-run is the default surface.
- Customized values never modified; orphans never deleted; user's own
  unknown sections invisible to sync.
- Comment-preserving surgery only — a whole-file rewrite is a failed
  implementation; refuse per-file when surgery can't be safe.
- Bundled policy read-only; one merge brain per table family (delegate);
  $0/stdlib; typed `CageError` reads; no ledger interaction.
- No commits.

## Acceptance criteria (self-check)

- [ ] All handoff §2 boxes; both safety tests present and green.
- [ ] A v0.16-shaped policy syncs to current: adds listed and applied,
      customized values intact, output categories exact.
- [ ] Second `--apply` byte-identical no-op; "already in sync" on a current
      file.
- [ ] Hints reworded; query answers; docs updated; tree review-ready; zero
      commits.
