> **ARCHIVED 2026-08-02 — implemented for v0.42 (unreleased).** History, not spec.
> Executed as scoped — see the paired handoff's archive header for the one naming
> correction (`manifest.read()`, not `manifest.read_imports`) and the one addition
> beyond scope (`insights chats` added to `test_floor.py`'s pinned view list). The
> CLAUDE.md architecture-flow/bullet edit was **proposed for review, not applied**, per
> this prompt's own instruction. Living spec: `cage/chats.py`'s module docstring +
> [FORMULAS.md §2.13](../../docs/FORMULAS.md) + `cage query chats-view`.

# Claude Code prompt: CHATS-VIEW — `cage insights chats`

**Model:** Sonnet — an additive, fully-specced feature with a decided design and an
explicit change-map (the one law amendment is pre-worded and test-pinned); no
diagnosis, no deletion, no substrate change.

**Progress:** 0% — single phase, not started (`cage/chats.py` does not exist yet).

You are implementing `cage insights chats` — a per-chat detail view (one row per
`(agent, surface, session)`, titled from the import manifest, numbers from the ledger
alone). The full spec is [docs/chats-view.handoff.md](chats-view.handoff.md); the
design of record is
[docs/proposals/chats-view.proposal.md](v0.42-chats-view.proposal.md). Read both
first — Definition of Done and Non-negotiables are binding.

## Context to load first

- `CLAUDE.md` (laws: determinism, method, honesty, $0/stdlib, docs discipline).
- `cage/adoption.py` — the structural template (summarize → render → render_csv,
  refusal discipline, CSV/`--since` handling). Copy its shape.
- `cage/importcmd.py` `_write_manifest` + `_lift_names`; `cage/manifest.py` (the
  contract you amend); `cage/report.py` (`_is_legacy_human`, `kiro_routed_line`);
  `cage/display.py`; `cage/csvout.py`; `cage/prices.py` (`call_usd_match`);
  `cage/cli.py` insights group + `cage/clicmds.py` `cmd_adoption` for wiring.

## Task

Build `cage/chats.py` + CLI wiring + tests + docs per the handoff. Mechanism (binding,
from the proposal): read calls (`--since` month-skip, legacy-human excluded+footnoted)
→ group by `(agent, surface, session)` summing calls/tokens_in/cached_in/
cache_write_in/tokens_out/premium + per-call `call_usd_match` repricing → join
`session_name` from `imports.jsonl` last-write-wins per `(agent, session)`, **labels
only** → sort tokens_in desc then session id, top-20 default with counted cut +
`--all` → render via `display.Display`/`Footer`, CSV twin from the same structure.

## Required workflow

1. **Explore** the named files before writing — confirm the adoption wiring shape and
   the manifest read helper actually available (`manifest.read_imports`).
2. **Plan** — list files you'll change and the test list; pause for confirmation.
3. **Implement incrementally** — module → wiring → renders → tests green at each step.
4. **Update docs to match** (handoff §9.5): README, CHANGELOG, FORMULAS, PLAN entry,
   explain_data `chats-view` entry, GLOSSARY, DOC-REGISTRY bumps, OPEN-WORK row
   removal after IMPLEMENTATION.md records the milestone, the `manifest.py` docstring
   carve-out sentence. **For CLAUDE.md: propose the edit and flag it for Arpit's
   review — do not silently rewrite it.** Archive the handoff/prompt pair to
   `work/archive/vX.Y-chats-view.{handoff,prompt}.md` on green (the archive-on-implement
   rule) and update the proposal's header pointer + `proposals/README.md`.
5. **Verify**: `just test` — full suite + new `tests/test_chats.py` + blessed goldens
   (`CAGE_BLESS_GOLDENS=1 pytest tests/test_output_spec.py` for the new fixtures
   only). Do not report done until 0 fail.

## Constraints (hard)

- stdlib only; derive-time only; ledger never rewritten; no new deps, no new config.
- Manifest read is **labels only** — a numeric cell influenced by `imports.jsonl` is
  a spec violation; the money-independence test must prove it.
- No `--team`, no MCP tool, no "lines suggested", no schema/substrate change, no
  manifest write-side change, no name backfill — all explicitly out of scope.
- `—` never enters CSV; UNPRICED never silently $0; no fabricated names; cut rows
  counted; kiro-IDE is ONE row (or the routed-line when absent).
- Do not modify: `schema.py` fields/enums, ledger write path, `_write_manifest`,
  wiring modules, `verbmap`.

## Acceptance criteria (self-check before finishing)

- [ ] Every Definition-of-Done box in the handoff checks true.
- [ ] Money-independence + determinism tests green; goldens blessed; `just test` 0 fail.
- [ ] Docs updated per §9.5; CLAUDE.md edit proposed, not applied silently.
- [ ] IMPLEMENTATION.md milestone appended; WORKLOG entry appended; OPEN-WORK row
      deleted; handoff/prompt archived with headers; CHANGELOG entry written.

## Tests

`tests/test_chats.py` covering: grouping math · title last-write-wins ·
delete-manifest ⇒ numbers byte-identical · determinism · truncation footer ·
kiro single-row + routed-line · legacy-human footnote · CSV parity · UNPRICED count.
Golden fixtures per state (empty · titled · untitled · truncated). Run `just test`.

## Guardrails

- Ask before: touching anything on the do-not-modify list, adding any flag not named
  here, or archiving docs early.
- If the manifest read helper differs from what the handoff assumes, or a law appears
  to conflict with the carve-out wording, STOP and ask — do not improvise a second
  amendment.
