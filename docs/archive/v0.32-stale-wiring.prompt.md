# Claude Code prompt: stale-wiring liveness — detect + heal

**Model: Sonnet.** A fully decided plan with an explicit change-map — the investigation is done,
the design is locked, the two open calls are made. This is additive detection + a well-specced
predicate refinement, not diagnosis. **One capture-critical caveat:** the `" import"`
substring-predicate swap can silently break capture if done wrong — treat that single change
with Opus-level care and stop if it's ambiguous. Everything else is mechanical.

Read `docs/stale-wiring.handoff.md` first — its Definition of Done (§2), Scope (§3), locked
Decisions (§5), and Non-negotiables (§6) are binding. This prompt is the execution wrapper.

## What you're building

A wiring artifact whose cage command uses a verb the current CLI rejects fails **silently**
(exit 1 → unmetered passthrough). Detect every such artifact against the **live parser**, heal it
on `cage setup`, and make a dead interceptor loud in `cage doctor`. This closes the root cause
behind F1.

## The locked decisions (do NOT re-open — handoff §5)

- **Detect with `cli.build_parser()`, not grep-`verbmap.REMOVED`.** The parser is ground truth
  for "will this exit 1"; `REMOVED` only supplies the replacement tail. (`adopt` is dead, installed,
  and not in `REMOVED` — a grep would miss it.)
- **Doctor scans user-level artifacts for liveness. No `--machine` flag.** `_portability` stays
  committed-only.
- **Assets: hash-compare vs `paths.bundled_data()`, advisory `·`, in `cage doctor` ONLY — never
  the `report` footer.**
- **Heal extends the existing migrate-and-print path. Foreign artifacts never touched. Retire the
  `" import"` substring coincidence for a parser predicate, PRESERVING `import-claude` /
  `import-codex` healing.**
- **Bundle Fix 3: wiring check renders above receipts; receipts line qualified when the
  interceptor is dead.**

## Required workflow

1. **Explore** the handoff §4 files first; verify the investigation's findings still hold (line
   numbers may drift).
2. **Plan** the change across the ~7 files + new `wiringscan.py`, and **pause for my confirmation
   before implementing.** In the plan, show exactly how the new parser-based predicate preserves
   `import-claude`/`import-codex` healing — that's the risky bit.
3. **Implement incrementally**, suite green at each step. Suggested order: (a) `wiringscan.py` +
   its unit/property tests, (b) doctor checks (read-only, additive), (c) the heal-side predicate
   changes + heal tests, (d) `bin/graphify` refresh, (e) Fix 3 doctor line, (f) docs + release.
4. **Re-bless goldens** for the new doctor lines, then `tools/docgen --target spec`. Never
   hand-edit spec blocks.
5. **Docs** per handoff §9.5. **CLAUDE.md: PROPOSE the diff, do not apply.**
6. **Run the full suite and paste actual output** (not "passing").

## The one thing to get exactly right

Replacing `claudewire._is_stale_import` / `paths.is_cage_import_command` (the `" import"`
substring) with a parser predicate must keep healing the two real historical cases. **Write the
test first:** plant `import-claude` and `import-codex` hooks, assert they still heal to
`import --agent …` after `agents.install`. If the predicate change makes that test ambiguous or
red, STOP and show me — do not guess. This is the single place a wrong move silently breaks
capture.

## Full test suite — HARD GATE

```
just test
python -m tools.dummyrepo
python -m tools.docgen --check
python -m tools.skillgen --check
just demo
```
Plus the new/extended: `test_wiringscan.py` (plant-detect-heal-idempotence, foreign-untouched),
the parser-liveness unit test, the `verbmap.REMOVED`→parser round-trip **property test**,
extended `test_portable_wiring.py`, a dummyrepo scenario, and a determinism byte-identity check
on a derived view. CI runs 3 OS × 3 Python — the path/parser code is the class that breaks on
Windows; keep it OS-stable.

## Constraints (hard — handoff §6)

- **$0 / stdlib-only.** No subprocess for the parser check — reuse `build_parser()`.
- **Detection is read-only + side-effect-free.** Never execute an artifact or run `cage import`.
- **Foreign (non-cage) artifacts: warn maybe, heal never.** Byte-identity test pins it.
- **Preserve `import-claude`/`import-codex` healing** (the caveat above).
- **No change to the ledger, schema, enums, method tagging, or any derived number.** Doctor is
  clock-exempt; derived views stay byte-identical — assert it.
- **Fail-open** on capture-path writes; new swallow sites logged under `CAGE_DEBUG`.
- **PII:** counts/commands/paths/hashes only.

## Out of scope — do NOT do
- **Codex removal** (separate, lands after this — do not strip Codex or touch `SURFACES`).
- **The `capture.log`/Jul-24 anomaly** (separate diagnosis).
- Version-stamping artifacts (rejected in §5).
- Creating a GitHub release — commit, stop for my go-ahead.

## Guardrails
- Never `git add -A`; don't commit `.claude/` or `graphify-out/`.
- No history rewrite. Don't edit the regression reports.
- Stop at the plan step and at the substring-predicate test if ambiguous.
- Ask before anything not in the handoff.

## Acceptance criteria
- [ ] `wiringscan.py` detects via `build_parser()`; assets via hash vs `bundled_data()`
- [ ] Doctor `wiring` check (user-level scanned, `✗` wired / `·` asset), above the receipts check;
      `_interceptor` now tests liveness (no false ✅)
- [ ] Heal rewrites dead verbs via `REMOVED` on `cage setup`; foreign artifacts byte-identical;
      copilot duplicate-entry bug fixed; `bin/graphify` refreshed on `--wire-only`
- [ ] `import-claude`/`import-codex` still heal — explicit test green
- [ ] `verbmap.REMOVED`→parser round-trip property test present and green
- [ ] Fix 3 receipts line qualified when interceptor dead
- [ ] Derived views byte-identical; goldens re-blessed + spec regenerated
- [ ] Full suite green (output pasted); docgen/skillgen `--check` green
- [ ] Docs updated; CLAUDE.md **proposed not applied**; version + test count bumped
- [ ] No codex/anomaly/ledger/schema change (`git diff --stat` proves the surface)

## Report back
`git status -sb`, `git log --oneline -3`, `git diff --stat`, the `just test` + `dummyrepo`
summaries, and confirmation the two must-preserve import cases still heal. Facts, not claims.

(Delete this file when done — throwaway.)
