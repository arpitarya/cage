> **ARCHIVED 2026-07-28 — implemented for v0.36 (unreleased).** History, not spec.
> creating `../cage-lab` fresh and running the matrix — done. Pending work from this cycle lives in
> [`docs/OPEN-WORK.md`](../OPEN-WORK.md); durable design belongs in
> [`docs/PLAN.md`](../PLAN.md). Never cite this file as current spec.

# Claude Code prompt: set up cage-lab and run the correctness matrix

**Model:** **Opus** — the independently-derived reference numbers are the
suite's entire value (a wrong reference silently blesses wrong behavior), and
deriving them from real logs is judgment work. Per cage's CLAUDE.md rubric this
is Opus, not Sonnet.

**Where to run:** Claude Code with access to BOTH siblings — `cage/` (under
test) and the parent dir where `../cage-lab` will be created. Suite code and
commits go in `cage-lab/`; only regression reports and build-log entries go in
`cage/`. **The `cage/` tree stays uncommitted** (Arpit's standing directive);
commit freely inside `cage-lab/` at each milestone.

---

You are creating the **cage-lab** sibling repo from scratch and running its
correctness matrix against the shipped cage. The spec is
`cage/docs/cage-lab-plan.md` (v3) — read it in full and treat it as binding;
`cage/docs/PLAN.md` §11 is the summary, `cage/docs/cage-lab-setup.handoff.md`
carries the Definition of Done. Arpit's frame, verbatim: values **absolutely
correct** (automatic + side-by-side eyeball), the solo/CLI/VS Code/combined
agent matrix, graphify combinations, and a playground that is his alone.

## Context to load first

1. `cage/docs/cage-lab-plan.md` — the whole spec (§1 laws, §2 layout, §3
   matrix, §4 auto-verify, §5 eyeball, §6 runner, §7 playground, §8 track 2)
2. `cage/docs/cage-lab-setup.handoff.md` — DoD + non-negotiables
3. `cage/docs/IMPLEMENTATION.md` — what v0.36 actually built (savings tree,
   manifest with always-on names, `migrate-savings`, id-deduped union)
4. `cage/samples/agent-artifacts/**/logs/real*/` — **the actual input files**
   (real sessions, real token counts, content-stripped). These are read in
   place, never copied or edited. NEVER take expected numbers from
   `derived-rows.json` — recount them yourself.

**Shape warning (learned the hard way, 2026-07-25):** the copilot VS Code title
is NOT on the `kind:0` record as an earlier plan draft claimed — it is a
`customTitle` patch record (`kind:1`, `k:["customTitle"]`) or
`kind:0.v.customTitle`, with the first request's `generatedTitle` as fallback;
an unnamed session is normal (88/143 real sessions named). Verify any shape
assumption in this plan against the real files or `cage/transcript.py` before
relying on it — and note this is exactly why the lab now uses real files
untouched: a wrong assumption can no longer be baked into an input.

## Task

1. **Scaffold** `../cage-lab` per plan §2 (`git init` if absent; if a checkout
   exists, replace its tree in one commit — history preserved). `python -m lab
   setup`: `uv build` the wheel from `../cage`, fresh venv, install, smoke
   `cage --version`. Stdlib-only lab code; zero `import cage` anywhere.
2. **ZERO mock data — use Arpit's real files as they are** (plan §2.1; this is
   the rule that decides whether the suite proves anything):
   - **No `fixtures/` directory. No copies. No edits.** Scenarios run
     `cage import --path <absolute path to the real file>` against
     `cage/samples/agent-artifacts/*/logs/real*/` (genuine sessions, real token
     counts, content-stripped only) and/or the live agent logs.
   - Where a scenario must exercise **auto-discovery** (no `--path`), put
     **read-only symlinks** to the real files in the scratch `$HOME` — same
     bytes, same inode. Never a copy.
   - **Prove you didn't touch them:** sha256 each source before and after every
     run and assert equality; a write-mode open on any path under `samples/` or
     a live agent log is an assertion failure in lab code.
   - **Real graphify only.** G-scenarios run the actual installed `graphify`
     against a real repo (cage's own `graphify-out/`). **Do not write a fake
     graphify binary.** Not installed ⇒ `NOT COVERED — graphify not installed`.
   - **No manufactured edge cases.** Truncated-tail: scan the real logs/ledger
     for a genuinely truncated final line and use it if one exists, else
     `NOT COVERED`. Untitled Copilot VS Code session: use a real one from the
     machine if present, else `NOT COVERED`.
   - **Any cell without real data prints `NO REAL DATA — cell not covered`** and
     is listed in the run summary. Never fabricate to fill a gap.
   - Then write `reference/<id>.json|md` — **your own parser's** recount of
     those same real files, with the arithmetic shown — **before** running cage
     against them. Independence comes from who computes, not from fake inputs.
     Never use cage's output or its `derived-rows.json`.
3. **Implement the matrix** (plan §3): M1–M3 solo · M4a/M4b CLI + M4c kiro-CLI
   as the honest refusal · M5a–M5c VS Code/IDE · M6 kiro+copilot combined ·
   G1–G5 graphify combos incl. native-shim dedupe and the migration-precision
   block (`migrate-savings`: receipts.jsonl byte-identical, attrib
   byte-identical before/after, second `--apply` copies 0).
4. **Auto-verify** (plan §4): three independent readbacks per scenario — your
   own raw-log recount, cage's report-json/CSV/rollup cross-checked against
   each other, lab-priced USD from the venv's bundled `cage.toml` (renamed from
   `policy.toml` in v0.36) read as
   data — all reconciled to the reference. Reads run twice, byte-identical or
   red. UNPRICED is asserted as UNPRICED, never $0.
5. **Build the eyeball surface** (plan §5) — this is Arpit's manual-verification
   surface; treat it as a deliverable, not a debug dump:
   - `python -m lab eyeball <id>` / `--all`, plus auto-written
     `runs/<id>/eyeball.md` on every run.
   - Contents: **ORIGINAL** (the real file's absolute path + "read in place,
     never modified" + before/after sha) / IMPORTED (the exact `cage import
     --path …` command) / LEDGER / MANIFEST / REFERENCE →
     the per-metric table (derivation with source line refs · reference · cage ·
     ✓/✗) → **the cited source lines quoted inline** → the matching ledger rows
     → a ready-to-paste side-by-side open command. He must be able to verify
     without opening anything, and jump straight to the real file when he wants.
   - `python -m lab source <id> [--open]` prints (or opens) just those paths —
     the fastest route from a scenario name to the real file to compare against.
   - **`runs/EYEBALL-INDEX.md`** on every run: scenario · what it proves ·
     verdict · eyeball path; failures sorted to the top with the failing metric
     and delta inline. The run's final output line is this file's absolute path.
   - Real numbers, so the surface does the arithmetic: show every addend with
     its source line plus a running sum, so each step stays checkable.
6. **Create the playground** (plan §7): pre-wired repo + venv + read-only
   symlinks to the real logs + `PLAYGROUND.md` cheat-sheet, own `CAGE_BASE`. Enforce in runner code that no
   runner path ever resolves under `playground/`; refresh only on explicit
   `python -m lab playground`.
7. **Wire track 2** on-demand: `lab regression` (plan §8 R-scenarios) and
   `lab labs` (L1–L6, strictly read-only on `CAGE_REAL_LEDGER`).
8. **Run everything and publish:** `python -m lab run --all` (exit 1 on red);
   `CAGE_REAL_LEDGER=~/.cage python -m lab labs`; `python -m lab publish` →
   dated baseline + `latest-*` + index row in `cage/docs/regression/`.

## Required workflow

- Plan-pause: post the file-level layout + the **input inventory** (`inputs.toml`:
  every scenario → the real file it will read, and which cells have no real data)
  + which reference files you'll write, and wait for confirmation.
- Order: setup → inputs+references → M-solo → M-surface → M6 → G-series →
  eyeball → playground → track 2 → full run → publish.
- Commit in `cage-lab/` at each green milestone; append a
  `cage/docs/IMPLEMENTATION.md` entry at each (setup · inputs+references ·
  matrix green · eyeball+playground · baseline published).
- Manually line-check the M2 and G5 eyeball tables yourself before declaring
  done — the eyeball surface must be trustworthy before Arpit uses it.

## Constraints (hard)

- **Never `import cage`** in lab code; drive the installed binary only.
- **References first**, derivations checked in; never regenerate a reference
  from cage's output; a cage-vs-reference disagreement you can't attribute →
  STOP and report.
- **ZERO mock data (plan §2.1).** Never invent or copy a log, a shape, a
  number, or a binary. Real files, read in place, sha-verified unchanged. No
  fake graphify. No manufactured edge cases. A cell without real data reports
  `NO REAL DATA — cell not covered` — fabricating to fill it is the one
  unforgivable failure here, because it goes green while proving nothing.
- **Do not modify cage source; do not commit in `cage/`;** append-only in
  `cage/docs/regression/`.
- Real `~/.cage`: read-only, labs only. Scenario runs: scratch HOME +
  `CAGE_BASE` per run.
- Honesty cells stay honest (claude `surface=""` with the WHY in its eyeball
  report; kiro has no CLI; kiro `ts` volatile) — never fabricate completeness.
- PII in published output: counts never content, tilde paths, no usernames.

## Acceptance criteria (self-check before finishing)

- [ ] Every DoD box in the handoff checked
- [ ] `python -m lab run --all` green twice in a row, results byte-identical
- [ ] Every asserted number traces to a pre-authored `reference/` file
- [ ] `runs/*/eyeball.md` exists for every scenario with inline source lines +
      ledger rows; you personally line-checked M2 and G5 before declaring done
- [ ] `runs/EYEBALL-INDEX.md` written, failures-first, printed as the run's last
      line
- [ ] Playground untouched by the runner (assertion + manual check)
- [ ] Baseline published in `cage/docs/regression/` with index row
- [ ] `cage/docs/IMPLEMENTATION.md` entries per milestone; `git -C ../cage
      status` shows the cage tree still uncommitted

## Guardrails

- STOP and ask if: a real file's shape disagrees with what the plan or parser
  expects; a reference-vs-cage delta can't be attributed to one side; any
  step would write to the real ledger, cage source, or `playground/` from the
  runner; or anything seems to require committing in `cage/`.
- A red scenario is fixed by fixing the lab's own bug or filing a cage finding
  — never by loosening the assertion.
