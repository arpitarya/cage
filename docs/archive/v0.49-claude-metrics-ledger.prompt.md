---
doc: prompt — claude-metrics: `.cage/ledger/claude/`, the per-chat Claude metrics ledger
status: ARCHIVED — implemented v0.49 (unreleased) on 2026-08-14
pair: [v0.49-claude-metrics-ledger.handoff.md](v0.49-claude-metrics-ledger.handoff.md)
---

> **ARCHIVED — implemented for v0.49 (unreleased) on 2026-08-14.** History, never
> current spec.

# Claude Code prompt: claude-metrics — the `.cage/ledger/claude/` metrics ledger

**Model:** Sonnet — additive, fully-specced capture work; every decision is locked in
the handoff and the change map names every insertion point. Escalate to Opus only if a
change-map fact turns out stale (that makes it diagnosis).
**Progress:** 100% — P0–P3 built and green (2026-08-14).

You are building a new ledger kind: `.cage/ledger/claude/chats-YYYY-MM.jsonl` — one
row per Claude Code **chat** per growth, carrying the correctly-folded per-chat
numbers (tokens in/out · cached in/out with the 5m/1h TTL split · thinking ·
server-tool counts · per-model totals · sidechain split · request/row counts). The
full spec is `docs/claude-metrics-ledger.handoff.md` — read it first; its
**Definition of done** and **Non-negotiables** are binding. Where this prompt and the
handoff disagree, the handoff wins. Store facts (which file, which row, which prop —
and THE DEDUP LAW) are grounded in
`docs/research/2026-08-13-claude-per-chat-usage-fetch-spec.md` — do not re-derive them.

The handoff's **§4 change map** was researched 2026-08-13 against HEAD and names every
file, function, and line anchor — follow it and the build is a zap. Re-verify a mapped
fact only if the code in front of you disagrees; then STOP and report.

## Before anything: the twin check

COPILOT-METRICS is a sibling build touching the same four files
(schema/paths/ledger/importcmd). If `make_copilot_metric` / `_ingest_copilot_metrics`
/ `ledger.copilot_metrics` exist at HEAD, **mirror those** — they are this design's
twins — instead of the older `make_credit`/`_ingest_credits` precedents. If another
session is building COPILOT-METRICS right now, STOP — the two builds must not run
concurrently.

## Context to load first

1. `CLAUDE.md`, then `work/OPEN-WORK.md` (triage — CLAUDE-METRICS is the item; note
   CLAUDE-DEDUP and CLAUDE-SUBAGENT-KEY are SEPARATE items you must not fix here).
2. `docs/claude-metrics-ledger.handoff.md` — the spec.
3. `docs/research/2026-08-13-claude-per-chat-usage-fetch-spec.md` — the store facts.
4. The precedents you will mirror (import, never reinvent):
   `cage/importcmd.py::_ingest_credits` + `import_claude` (l. 383–433, the call site) ·
   `cage/paths.py::savings_shard`/`shard`/`savings_shards` ·
   `cage/ledger.py::credits` (last-write-wins collapse) ·
   `cage/schema.py::make_credit` (deterministic value-folded id) ·
   `cage/transcript.py::parse_calls`/`_usage_to_row` (READ-ONLY — do-not-touch).
5. Contracts: `tests/test_floor.py` + goldens (pass untouched) ·
   `tests/test_debug_coverage.py` (every new swallow site logs under `CAGE_DEBUG`) ·
   `tests/test_queue_honesty.py`.

## Build order (phases — suite green after each)

- **P0 substrate**: `schema.make_claude_metric` + `CLAUDE_METRIC_FIELDS` (NO credits
  field — the research doc's firm no) · `paths.Footprint.claude_dir/claude_shard/
  claude_shards` + `shard()` routing kind `"claude"` ·
  `ledger.claude_metrics_raw` + `claude_metrics` (collapse last-write-wins per
  `session`, winner max `(requests, tokens_in+tokens_out, id)`).
- **P1 the fold**: `transcript._fold_claude_chat` + `parse_claude_chat_metrics` —
  THE DEDUP LAW: fold key `(requestId, message.id)`, **last occurrence wins**, uuid
  fallback for legacy rows; chat key = the row's own `sessionId` (filename stem
  fallback); sidechain rows join the parent chat AND split into
  `sidechain_tokens_*`; `metric_id = "clm_" + sha1(session|payload)[:16]`
  (values folded → growth appends, unchanged dedupes). Whitelist read: envelope +
  `message.usage` only — never `message.content`, `summary`, or `tool-results/`.
- **P2 wiring**: `importcmd._claude_session_filesets` (a changed subagent file pulls
  its WHOLE session fileset — partial totals are the failure this kills) ·
  `_ingest_claude_metrics` (seen-set from `claude_metrics_raw`, fail-open,
  `debuglog` events) · call site in `import_claude`'s per-source loop on the same
  `files` list (no rescan). Metrics rows never touch the call `seen` set.
- **P3 surface + docs**: doctor advisory `claude-metrics` line (found/absent · raw
  vs collapsed counts · >25-day retention nudge) · `explain_data` entry
  `claude-metrics` · every §9.5 doc box (incl. `docs/claude-capture.md` — the
  standing claude-numbers one-pager fires on this change).

## Constraints (hard)

- **Do NOT touch** `transcript.parse_calls` / `_usage_to_row` or anything in the
  calls path — CLAUDE-DEDUP and CLAUDE-SUBAGENT-KEY are separate OPEN-WORK items.
  A test must pin `parse_calls` output byte-identical on the new fixtures.
- Counts-never-content: usage numbers + envelope ids only; no chat titles; `project`
  = cwd basename only.
- No credits field, ever; never derive credits/USD from tokens at capture.
- Capture-only: NO derived view reads the kind; `report` + `insights chats` stdout
  byte-identical with the `claude/` tree present vs deleted (write this test).
- Fail-open per file/fileset, `CAGE_DEBUG`-logged. $0 stdlib. Append-only: ids fold
  values; never rewrite a shard. No cleanup class for the dir.
- Do NOT edit the verified fixture corpus under `tests/fixtures/transcripts/` —
  build NEW controlled fixtures (include a `subagents/` layout fixture).

## Required workflow

1. **Triage** `work/OPEN-WORK.md`; verify the CLAUDE-METRICS line exists; run the
   twin check above.
2. **Explore** the §4-mapped code; on any stale fact, STOP and report before coding.
3. **Implement by phase**, `just test` green each time; append
   `work/IMPLEMENTATION.md` per phase.
4. **Docs in the same change** — every handoff §9.5 box; `CLAUDE.md` edits are
   **proposed and flagged for Arpit**, never silently applied.
5. **Archive on implement** — green ⇒ move the pair to
   `docs/archive/vX.Y-claude-metrics-ledger.*` ("implemented for vX.Y (unreleased)"
   if untagged), update `docs/README.md` + `docs/archive/README.md`, set this
   Progress line to 100% in the move, delete the OPEN-WORK line, add the two
   scope-out lines (read surface · CSV export).
6. **Verify**: full suite green with NO golden re-bless (a golden diff means you
   broke capture-only — STOP) · `cage import --agent claude` twice on a scratch
   ledger → second run appends 0 · `work/WORKLOG.md` entry with `Cost:` from
   `cage report` (or `Cost: unmeasured — <why>`).

## Acceptance criteria (self-check)

- [ ] Every Definition-of-done box in handoff §2 true.
- [ ] `tests/test_claude_metrics.py` covers the handoff §9 list — especially: the
      3-duplicate-rows fold (`raw_rows=3, requests=1`, last `output_tokens` wins) ·
      subagent join via row `sessionId` · fileset regroup on a subagent-only change ·
      the calls-path byte-identity pin · the report/chats byte-identity test.
- [ ] Re-import idempotent; growth appends + collapse wins; never summed.
- [ ] All §9.5 docs done; pair archived; OPEN-WORK truthful (CLAUDE-DEDUP /
      CLAUDE-SUBAGENT-KEY still listed — you did not fix them).

## Guardrails

- STOP and ask if: a §4 fact is stale · a golden or `test_floor` diffs · another
  session holds the import lock or is mid-build on COPILOT-METRICS · you feel the
  need to read `message.content` or a summary title "just to key rows" (that is the
  design telling you no).
- Never delete ledger data; never rewrite append-only files.
