# Claude Code prompt: CLI tiering (plan Phase 3 — own release)

You are implementing the CLI reorganization. Specs, in authority order:
**`docs/cli-tiering.handoff.md`** (binding) → the plan's Phase 3 block in
`docs/output-and-simplification.plan.md` (the mock `--help` is the verbatim
help target). **Sequencing gate:** `output-honesty` must be implemented in
this tree — STOP and ask if not. **No commits, tags, pushes, publishes.**

## Context to load first

Handoff §4 list: plan Phase 3 block, `cli.py` + `clicmds.py`, `initcmd.py` +
`setupcmd.py`, `importcmd.py`, grep `"cage "` across `cage/` for emitted hint
strings, `tools/skillgen/fragments/`, `docs/cli-output-spec.md`, `CLAUDE.md`,
the graphify interceptor path (`runshim.py`, `adoptcmd.py`,
`data/shims/graphify`).

## Task, in order

1. **Explore** the parser/dispatch and build the complete old→new verb map;
   inventory every emitted `cage …` hint string and every wired-file verb
   reference. Show me the map and both inventories.
2. **Capture behavior-freeze goldens** for every moving verb UNDER ITS OLD
   NAME first (output + exit code, representative flags).
3. **Plan** the parser regrouping (same run functions, new subparsers), the
   `render.cmd()` hint helper retrofit, the init→setup merge. Pause for my
   confirmation.
4. **Implement:** tier-1 help (plan mock, verbatim golden) · groups
   (`insights`, `human`, `data`; existing three untouched) · init merged into
   setup (scaffold unconditional) · old verbs removed with the
   error-with-directions map (exit 1, static dict, drives the CHANGELOG table
   too) · `hook-*` hidden from help, still callable · argparse
   prefix-matching disabled/verified · graphify interceptor seam updated and
   end-to-end tested.
5. **Regenerate the world:** skillgen fragments → all four hosts' assets ·
   every hint string through `render.cmd()` (grep proves zero stale) · docs
   sweep (README commands, cli-output-spec headings, pricing/restricted/
   portable, full-test-plan tier-1 smoke section) · query concept entries.
6. **Verify:** old-vs-new golden byte-diffs all identical · unknown-verb map
   tests · help golden · hint grep clean · wired-files grep clean · CSV
   untouched · dummyrepo updated + green · `just test` green.
7. **Docs:** CHANGELOG with the generated mapping table (this is a
   breaking-change release — say so plainly) · CLAUDE.md edit
   proposed-not-applied.

## Constraints (hard)

- Zero behavior change beyond the verb map — the old-name goldens are the
  contract; if any diff isn't explainable by the usage/program line, stop.
- Recorded-data readers untouched; `hook-*` callability untouched; MCP tool
  names untouched.
- The removed-verb handler never silently runs the new command — error with
  directions only.
- $0/stdlib; rendered assets via skillgen only; no commits.

## Acceptance criteria (self-check)

- [ ] Every handoff §2 box; `--help` matches the plan mock byte-for-byte.
- [ ] All moved verbs: old-name golden == new-name output; old name errors
      with the correct direction.
- [ ] Zero stale `cage <old-verb>` strings anywhere (grep in code, docs,
      rendered assets, wired files).
- [ ] Interceptor end-to-end green; tier-1 smoke green; tree review-ready;
      zero commits.
