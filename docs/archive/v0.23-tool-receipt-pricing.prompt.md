> **Archived — shipped in v0.23 (see [CHANGELOG](../../CHANGELOG.md)). History, not spec.**

# Claude Code prompt: tool-receipt pricing ladder (cage)

You are implementing the derive-time pricing ladder for call-less token
receipts. The spec is **`docs/tool-receipt-pricing.handoff.md`** — read it
first; its Definition of Done and Non-negotiables are binding. Run against a
clean reviewed tree (ask if dirty). **No commits, tags, pushes, publishes.**

## Context to load first

The handoff §4 list: `convert.py`, `prices.py`, `roi.py`, `attribution.py`,
`verdict.py`, `taskgroup.py` (reuse its join — do not reimplement),
`graphifymeter.py`, `policy.py`, `explain_data.py`, `tools/dummyrepo/run.py`,
`CLAUDE.md`, `docs/pricing.md`.

## Task

1. **Explore** the receipt→USD path end-to-end; confirm where call-less
   receipts fall to $0 today (roi ~line 26, attribution, verdict's inputs).
2. **Plan** — files you'll touch, where the ladder lives (one place), how the
   task/session join structure threads to consumers without per-receipt
   ledger scans. Pause for my confirmation.
3. **Implement the ladder** (handoff §2.1): policy `[tools.<tool>] price_at`
   → **whole-task** dominant model (decided — never after-ts filtering; see
   handoff §10; session fallback; deterministic tie-breaks: tokens_in sum,
   then call count, then lexicographic) → unpriced. Linked receipts
   byte-identical. USD keeps the receipt's `method`, rung footnoted in text
   views and a column in CSV; UNPRICED summary covers unpriceable tool
   receipts with a **runnable** fix hint; `verdict` names the rung.
3b. **Implement `cage prices route-tool <tool> --to provider/model`** on
   `pricestoml`'s managed-block machinery, mirroring `prices alias`
   (validation, idempotent re-run, dangling-target warning in
   `prices list`/doctor). Fence per handoff §3: if the `[tools.*]` table
   shape doesn't extend cleanly, STOP and show me the cost — do not silently
   fall back to a manual-edit hint.
4. **Explain + docs:** `cage query receipt-pricing` (live values), pricing.md
   section, CHANGELOG in-tree (call out that historical roi/attrib/verdict
   numbers change — tokens now priced), README what's new, CLAUDE.md edit
   proposed-not-applied, skillgen regen + `--bless` if CLI text changed.
5. **Verify:** goldens for all three rungs + tie-break + fleet-merge
   (unresolvable call id enters the ladder) + zero-call task → session →
   refusal; legacy linked-receipt byte-identity; determinism double-runs;
   `cage demo` §4.4 unchanged; dummyrepo scenario added and green;
   `just test` green.

## Constraints (hard)

- One ladder implementation; every consumer routes through it.
- Derive-time only — no receipt row is ever written or rewritten.
- Method sacred: never `measured`, rung always visible, rung 3 refuses
  rather than defaults.
- Deterministic total ordering on ties (tested).
- $0/stdlib; no schema changes; write path untouched; four agents unaffected.
- Handoff §10 questions are RESOLVED (route-tool ships; whole-task dominant)
  — implement as decided; the only escalation is the §3 pricestoml fence.
- No commits.

## Acceptance criteria (self-check)

- [ ] Every handoff §2 box satisfied; goldens exact; double-runs identical.
- [ ] A seeded graphify session shows dollars in `roi`/`attrib`/`verdict
      graphify` with the correct rung footnote; an unpriceable one shows the
      UNPRICED hint instead of a number.
- [ ] Linked receipts and `cage demo` outputs byte-identical to before.
- [ ] Query entry answers with live values; docs updated; tree review-ready,
      zero commits.
