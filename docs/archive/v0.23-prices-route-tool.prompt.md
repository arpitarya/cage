> **Archived — shipped in v0.23 (see [CHANGELOG](../../CHANGELOG.md)). History, not spec.**

# Claude Code prompt: `cage prices route-tool` (completes the v0.23 spec)

You are adding the managed writer for `[tools.<tool>] price_at` routes and
upgrading the tool-receipt UNPRICED hint to a runnable command. The spec is
**`docs/prices-route-tool.handoff.md`** — read it first; Definition of Done
and Non-negotiables are binding. This executes an already-debated decision
(`docs/archive/v0.23-tool-receipt-pricing.handoff.md` §10) — do not re-defer
it. **No commits, tags, pushes, publishes.**

## Context to load first

`cage/pricescmd.py` (how `alias` registers + writes) · `cage/pricestoml.py`
(managed-block machinery — the extension point) · `cage/receiptprice.py`
(`UNPRICED_HINT`, `routes()`, `dangling()`) · the ⚠ rendering in
`roi.py`/`report.py` · `explain_data.py` (`receipt-pricing`, `prices-cli`) ·
`docs/pricing.md` · `tools/dummyrepo/run.py` S14 · `CLAUDE.md`.

## Task

1. **Explore** how `prices set`/`alias` write the managed block and how they
   handle a user's hand-added duplicate outside it — mirror those behaviors
   exactly.
2. **Plan** (files touched, the `[tools.*]` block shape) and pause for my
   confirmation. **Fence:** if `pricestoml` needs surgery rather than
   extension for the new table shape, STOP and show me the cost — never
   silently defer.
3. **Implement:**
   - `cage prices route-tool <tool> --to <provider>/<model>` — validated
     (single-token tool, provider/model format, `price_match` check on the
     target with a printed warning when dangling, never a refusal),
     idempotent, before/after printed, bundled policy untouched.
   - `--remove` — idempotent managed-block deletion.
   - `receiptprice.UNPRICED_HINT` → runnable form; roi/report ⚠ lines render
     it with the real tool name substituted.
4. **Docs/query/skillgen:** pricing.md swaps hand-edit for the command;
   `receipt-pricing` + `prices-cli` query entries updated; skillgen fragments
   regen + `--bless`; CHANGELOG in-tree ("completes the v0.23 debated
   spec"); CLAUDE.md edit proposed-not-applied.
5. **Verify:** tests per handoff §2 including the literal
   hint-is-runnable end-to-end (copy printed hint → run it → rung 1 dollars
   appear); S14 sets its route via the verb; determinism double-runs;
   `just test` green; `cage demo` byte-identical.

## Constraints (hard)

- `receiptprice.resolve` and every derive path untouched except the hint
  string.
- Bundled `data/policy.toml` ships no routes (assert in a test).
- Typed `CageError` for bad input; write paths idempotent.
- $0/stdlib; rendered assets via skillgen only; no commits.

## Acceptance criteria (self-check)

- [ ] All handoff §2 boxes; the copy-paste hint test passes literally.
- [ ] route-tool/--remove idempotent; dangling warns and is visible in
      `prices list` + doctor as before.
- [ ] Ladder behavior and demo output unchanged; only the hint text and the
      new verb differ.
- [ ] Docs/query/skillgen updated; tree review-ready; zero commits.
