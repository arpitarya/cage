> **ARCHIVED — resolved for v0.39 (unreleased), 2026-08-01.** Part 1 executed as
> written (CLAUDE.md rule). **Part 2 must NOT be built — its premise is false.** Ignore
> everything below "## Part 2" in this file; it describes a state line that already
> exists via `cmd_overview` (`cli.py:651`). See the closing note at the bottom.

# Claude Code prompt — structural debt (DEBT)

**Model: Sonnet.** One rule to write down, one small feature with an explicit shape.
Escalate to Opus only if the state line cannot be made fast enough without touching a
read path that capture-on-read would then fire on.

**No paid LLM calls. The cage tree stays uncommitted** (commits in `cage-lab` only).

## Read first

- [structural-debt.handoff.md](structural-debt.handoff.md) — both parts, and the
  **correction** to the proposal
- `cage/cli.py` — `_ROOT_HELP` (line ~23) and `main()` (~614)
- `CLAUDE.md` — the existing fix-on-contact rules, for placement and voice

## Part 1 — the split-on-contact rule (~15 min, no code)

Add to CLAUDE.md beside the other fix-on-contact rules: **`paths.py` splits on contact,
never wholesale.** Named seams: `routing.py` · `logsources.py` · `agenthomes.py` ·
`footprint.py`. Pure moves with re-exports; no behaviour change. Include the clause
CODEX-OUT earned: **a deletion and a move never share a diff** — if the touching change
is a removal, do the removal and leave the seam.

**Do not split anything now.**

## Part 2 — a state line above the help

**First, verify the correction yourself:** run `cage` with no args. It prints
`_ROOT_HELP`, a curated one-screen brief — **not** argparse usage. The proposal said
otherwise and was wrong. If what you observe differs from this, stop and report.

Add **one line above** the unchanged help:

```
ledger ~/.cage · 1,204 calls · last import 3h ago · ⚠ 12 unpriced
```

- **Cheap or degraded** — bare `cage` stays instant. If the full read isn't trivially
  fast, print just the ledger path rather than slowing the command. Say which you did.
- **Honest when empty:** no ledger ⇒ `no ledger yet — run cage setup`, and nothing else.
  **Never print fabricated zeros.**
- **Bare `cage` must write nothing** — no import, no cursor, no state file.
  **Capture-on-read stays OFF.** Assert this with a test; it is the one way this feature
  could do real harm.
- **Fail-open:** unreadable ledger ⇒ help only, exit 0, no traceback.
- Tilde-relative path (`importcmd._tilde`), so the golden is machine-portable.

## Hard constraints

- `_ROOT_HELP`'s content is unchanged — you are adding a line, not redesigning help.
- stdout is the deterministic surface; the state line is part of it, so it needs a golden.
- stdlib only; fail-open; no commits in `cage`; don't touch `docs/regression/**`.

## Acceptance criteria

- [ ] CLAUDE.md carries the rule with all four seams and the deletion-vs-move clause
- [ ] Bare `cage` prints the state line + unchanged `_ROOT_HELP`, exit 0
- [ ] Empty-ledger and unreadable-ledger cases behave as specified
- [ ] **A test proves bare `cage` writes nothing** (no ledger mutation, no cursor, no
      capture-on-read)
- [ ] Golden for bare `cage`; `just test` green; re-blessed goldens named
- [ ] `proposals/structural-debt.md` archived per the lifecycle, header naming the
      living specs (CLAUDE.md for the rule, the golden for the feature)
- [ ] IMPLEMENTATION / WORKLOG / DOC-REGISTRY / OPEN-WORK updated

## Handback

Whether bare `cage` matched the correction · the state line as printed (paste it) ·
whether you took the full read or the degraded one and why · the no-write test ·
re-blessed goldens.

---

## ⚠️ CLOSING NOTE 2026-08-01 — do not execute Part 2

An agent executing this prompt cold ran the exact verification step above ("run `cage`
with no args") and found the premise false a **third** time: bare `cage` runs
`clicmds.cmd_overview`, not `_ROOT_HELP`, and it already prints a state line (tokens ·
calls · unpriced · last-import) with deliberate capture-on-read. `_ROOT_HELP` only shows
on `cage --help`. Presented four rescoping options; the decision was **leave Part 2
alone entirely — closed, not built.** Part 1 (the CLAUDE.md rule) is implemented. No
code changes belong to this prompt going forward. Full record:
`docs/WORKLOG.md` (2026-08-01, "DEBT Part 2 CLOSED").
