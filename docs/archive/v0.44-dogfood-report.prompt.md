> **ARCHIVED 2026-08-02 — implemented for v0.44 (unreleased).** History, not spec.
> Executed as scoped — see the paired handoff's archive header for the one deviation
> (`cage insights attrib` omitted, real ledger holds only the `cage demo` seed).
> P0's stop condition fired correctly on first attempt (`docs/dogfood/` did not yet
> exist) and Arpit then ran P0 himself in-session before P1–P3 proceeded, matching
> this prompt's own ordering rule. The CLAUDE.md steering edit was **proposed for
> review, not applied**, per this prompt's own guardrail. Living spec:
> `docs/dogfood/README.md` · `tests/test_dogfood_freshness.py`.

---
doc: prompt — DOGFOOD (measured on itself)
status: ready to run
raised: 2026-08-02
---

# Claude Code prompt — DOGFOOD

**Model:** **Sonnet** — the plan is decided and the change-map is explicit; this is
additive doc/test work with no diagnosis and no substrate contact.

**Progress:** 100% — all four phases built and green 2026-08-02 (P0 Arpit's hands ·
P1 home+pointer · P2 guard · P3 doc sweep+archive). Four phases: **P0** (Arpit's
snapshot, prerequisite) ·
**P1** (`docs/dogfood/` home + README pointer) · **P2** (the guard test) · **P3** (doc
sweep + archive). P0 is **not yours** — it must already exist when you start.

---

You are publishing cage's own ledger numbers so they can never silently rot. Full spec:
**[docs/dogfood-report.handoff.md](dogfood-report.handoff.md)** — read it first and
treat its Definition of Done (§2) and Non-negotiables (§6) as binding.

## Stop condition — check this before anything else

`docs/dogfood/<YYYY-MM-DD>.md` must already exist, containing Arpit's real command
output from the dev machine (P0).

**If it does not exist, STOP and say so.** Do not seed a ledger, do not run
`cage report` against whatever ledger this machine has, do not write example numbers,
do not write a placeholder. **ZERO dummy data is the entire premise of this feature** —
a fabricated number here is worse than shipping nothing.

## Context to load first

- `docs/proposals/dogfood-report.md` — the design of record.
- `docs/regression/README.md` + a dated file in `docs/regression/` — **the pattern you
  are mirroring**. Dated filenames, a `latest-*` mirror, an append-only index. Do not
  invent a new shape.
- `README.md` lines 12–55 — line 16 is the placeholder you replace; **line 53 you must
  not touch**.
- `docs/DOC-REGISTRY.md`, `docs/README.md`, `docs/OPEN-WORK.md`.

## Task

**P1 — the home and the pointer**

1. `docs/dogfood/latest.md` mirroring the newest dated snapshot.
2. `docs/dogfood/README.md` — index + the append-only rule, mirroring `regression/`.
3. Replace README line 16 (`▶ Demo GIF coming soon.`) with a pointer to `latest.md`
   that carries **no version number and no date**. It must never need editing again.

**P2 — the guard**

`tests/test_dogfood_freshness.py`. Two assertions, both required:

- `latest.md`'s frontmatter `snapshot_date` is ≤ **60 days** old.
- `snapshot_date` **equals the date in the newest `docs/dogfood/<date>.md` filename.**
  Without this, the guard is satisfiable by editing one line and never re-running the
  numbers — which is the checklist-line failure in a new costume.

It reads **frontmatter only, never numbers** — that is why it can run in CI with no
ledger. Missing or empty `docs/dogfood/` ⇒ **fail**, never skip.

The failure message must state, in plain words, that this is a calendar-triggered
freshness reminder rather than a code regression, and that `CAGE_SKIP_DOGFOOD_FRESHNESS=1`
skips it when bisecting or building an old tag. Make that env escape work.

**P3 — the doc sweep** — handoff §9.5, all of it.

## Required workflow

1. **Explore before writing.** Confirm the `regression/` pattern by reading it, and
   confirm nothing globs `docs/*.md` in a way your new directory would disturb.
2. **Plan** — list the files you'll add/change, and pause for confirmation.
3. **Implement incrementally**, suite green at each step.
4. **Update docs to match** — handoff §9.5. If CLAUDE.md warrants a line, write
   `docs/proposals/steering-edits-pending.proposal.md` and **stop there**; never edit a steering file
   silently (the HR-CLAUDEMD / CC-CLAUDEMD precedent).
5. **Verify:** `just test`. Refresh the count in README and in CLAUDE.md's `just test`
   comment. Do not report done while a doc contradicts the tree.

## Hard constraints

- **Never author a number.** Every figure is Arpit's, pasted verbatim, method tags
  intact. Gross keeps its gross note; `UNPRICED` is shown, not trimmed; refusals are
  relayed unsmoothed.
- **Do not modify:** anything under `cage/` · `data/` · the golden fixtures · README
  line 53.
- **$0 / stdlib only** — no YAML library for frontmatter; string handling or an
  existing helper.
- Do **not** add `cage insights chats` or a `--project` breakdown to any snapshot: chat
  titles and working-dir basenames leak private project names, and this repo is public.
- Do **not** write a CHANGELOG entry — nothing user-facing ships here.

## Acceptance self-check

- [ ] `docs/dogfood/{<date>,latest,README}.md` present; snapshot untouched by you
      except for being mirrored.
- [ ] README pointer carries no version and no date; line 53 unchanged.
- [ ] Guard fails on: >60 days · frontmatter/filename date mismatch · missing directory.
      Skips under `CAGE_SKIP_DOGFOOD_FRESHNESS=1`.
- [ ] `just test` green; counts refreshed in README + CLAUDE.md.
- [ ] IMPLEMENTATION entry written **before** the `DOGFOOD` row is deleted from
      OPEN-WORK; proposal archived to `docs/archive/v0.44-dogfood-report.proposal.md`
      and moved to **Graduated** in `proposals/README.md`; this pair archived alongside
      it; DOC-REGISTRY rows bumped.
- [ ] WORKLOG entry appended; INTERVIEW refreshed if direction or constraints changed.

## Guardrails

- Ask before: deleting anything, touching `cage/`, re-blessing a golden, or changing
  any committed wiring file.
- If P0's snapshot is missing, ambiguous, or looks like it contains something beyond
  counts, **STOP and ask**. Guessing here defeats the feature.
- **Do not commit.** Leave the tree dirty for Arpit's review (standing directive).
