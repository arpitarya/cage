# Claude Code prompt: output honesty (plan Phases 1+2 + 5.6 generators)

You are implementing the output-honesty work. Specs, in authority order:
**`docs/output-honesty.handoff.md`** (Definition of Done, Non-negotiables —
binding) → **`docs/cli-output-spec.md`** (the verbatim rendering target) →
`docs/output-and-simplification.plan.md` Phases 1/2/5.6 (rationale).
**Sequencing gate:** if `prices-route-tool`, `pricing-freshness`, or
`policy-sync` are unimplemented in this tree, STOP and ask. **No commits,
tags, pushes, publishes.**

## Context to load first

The handoff §4 list: `cli-output-spec.md` (read every mockup), `render.py`,
`report.py`, `matrix.py`, `humanview.py`, `trend.py`, `roi.py`,
`csvout.py` (FROZEN — schema and values), `explain_data.py`,
`tools/skillgen/` (generator pattern), `docs/formulas.md`, `CLAUDE.md`.

## Task, in order

1. **Explore** the render layer; inventory every place a `$` value, footnote,
   ⚠ block, or advice line is emitted today.
2. **Plan** — the display-context object design, files touched, golden-fixture
   layout, generator tool layout. Pause for my confirmation.
3. **Render changes** (spec-verbatim): tokens-default + `--usd`/`[display]
   usd` on report (all views + bare overview — handoff §10 default: yes) and
   matrix · `—` for unpriced everywhere in text · signal-gated saved/net with
   the one-line explanations · matrix token-grid-always + `--usd` cost column
   or one-line unavailability · 0-call rows dropped · footnote/⚠/advice
   dedupe, fixed bottom order · staleness-gated last-import line · kiro
   input-only caveat · `--all-columns` (composes with `--usd`; test both
   orders).
4. **Golden fixtures:** one seeded-ledger golden per spec mockup (every
   command × state in the spec). Negative-net-with-receipts visibility gets
   its own named test.
5. **Generators (build-time, `tools/`, never in wheel, each with `--check`):**
   spec code blocks ← golden fixtures · `docs/formulas.md` ← `explain_data.py`
   calculation entries (hand-written prose survives via template anchors) ·
   bundled policy.toml formula comments ← same registry. Wire all three
   `--check`s into CI beside skillgen's.
6. **Papercut riders:** `--scope` empty-slice message; copilot/auto alias
   example comment audit.
7. **Docs:** flip `cli-output-spec.md` status to live + README link beside
   the CSV contracts (same change — binding) · query entries for `--usd`/
   gating/`—` · CHANGELOG in-tree calling out the text-output break ·
   CLAUDE.md proposed-not-applied · skillgen fragments regen + `--bless`.
8. **Verify:** all goldens green · CSV byte-identity suite vs pre-change
   (schema AND values) · three `--check`s clean · determinism double-runs ·
   dummyrepo scenario text updated · `cage demo` re-pinned once (CHANGELOG
   notes it) · `just test` green.

## Constraints (hard)

- Rendering only — no computed value changes (CSV byte-identity is the proof;
  if CSV moves, you changed math: stop).
- Negative net with receipts always renders; `—` never enters CSV data;
  money-native views' dollars untouched.
- Gating/advice decided by ledger content, never wall clock.
- One display-context implementation — no per-view copies of gating logic.
- $0/stdlib; generators build-time only; no commits.

## Acceptance criteria (self-check)

- [ ] Every handoff §2 box; every spec mockup has a passing golden.
- [ ] Spec status flipped + README linked in this change.
- [ ] CSV byte-identical pre/post; negative-net test present and green.
- [ ] Three generators + `--check`s in CI; regenerating changes nothing.
- [ ] Tree review-ready; zero commits.
