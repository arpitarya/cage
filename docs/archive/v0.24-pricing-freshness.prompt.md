> **Archived — shipped in v0.24 (see [CHANGELOG](../../CHANGELOG.md)). History, not spec.**

# Claude Code prompt: pricing freshness + complete Anthropic/OpenAI tables

You are implementing the pricing-freshness work. The spec is
**`docs/pricing-freshness.handoff.md`** — read it first; Definition of Done
and Non-negotiables are binding. If the `prices-route-tool` pair is still
unimplemented in this tree, STOP and ask which runs first (they touch the
same hint strings). **No commits, tags, pushes, publishes.**

## Context to load first

Handoff §4 list: `gitcommithook.py` + `hooks.py` (post-commit ride-along),
`prices.py`/`policy.py`/`pricestoml.py` (`[meta]`, `price_match`),
`doctorcmd.py`, `report.py` (footer conventions), `constants.py`
(policy-preferred fallback pattern), `data/policy.toml` (row citation
convention), `.github/workflows/` (publish untouched),
`tests/fixtures/transcripts/`, `CLAUDE.md`, `docs/pricing.md`.

## Task

1. **Explore** the post-commit path and where doctor/report render pricing
   recommendations today; confirm the fixture-corpus model ids.
2. **Plan** (files touched, where `freshness()` lives) — pause for my
   confirmation.
3. **A — the check:** one `freshness(root, pol) -> list[str]` helper; three
   local signals (project-vs-bundle meta drift → `cage prices sync` hint;
   bundle age vs `[prices] stale_days` / `constants.PRICES_STALE_DAYS`
   default 45 → newer-release hint; UNPRICED presence → existing runnable
   hints). Rendered identically by post-commit (fail-open, print-only,
   silent-when-clean, wall clock allowed), `cage doctor` (always shown), and
   the report footer (actionable-only; **data-relative age** — newest ledger
   `ts`, never the wall clock, so derived output stays deterministic).
   `cage query prices-freshness` entry with live values.
4. **B — the tables:** web-research Anthropic and OpenAI official pricing
   pages NOW, at build time. Populate `data/policy.toml`: every GA model
   plus recent-history (2025–26-billable) models, input/output/cache_read
   per Mtok, one source-URL + retrieval-date comment per row. Extend
   aliases/family rows for naming variants (don't touch the matcher). Bump
   `[meta] prices_version/prices_date`. Do not guess a single rate — a model
   whose price you cannot source gets no row and a note in your summary,
   not an invented number.
5. **C — the CI nag:** scheduled weekly workflow (new file, publish workflow
   untouched): reads bundled `prices_date`, past `stale_days` upserts ONE
   pinned issue ("bundled prices are N days old — re-verify cited sources");
   reopens if human-closed; never fetches or parses vendor rates.
6. **Verify:** tests per handoff §2 (goldens for each signal, silent-clean,
   swallowed-hook-failure + debug trace, stale_days override, data-relative
   math, fixture-corpus zero-`none` through `price_match`, sync pickup,
   dummyrepo backdated-meta end-to-end); determinism double-runs;
   `just test` green; skillgen `--check` clean.
7. **Docs:** CHANGELOG in-tree, pricing.md freshness section, README
   one-liner, CLAUDE.md proposed-not-applied.

## Constraints (hard)

- No network in any cage code path — your research is build-time; CI nags
  humans, never fetches rates; no auto-applied updates.
- Print-only and exit-0 everywhere; the hook path swallows its own failures.
- Derived views clock-free (data-relative age); post-commit may use today.
- Bundled policy read-only at runtime; cited rows only; $0/stdlib.
- One `freshness()` implementation, three render surfaces.
- Handoff §10 (CSV footer) is mine — default text+doctor only.
- No commits.

## Acceptance criteria (self-check)

- [ ] All handoff §2 boxes; a backdated-meta commit prints the line, sync
      silences it.
- [ ] Every fixture-corpus anthropic/openai id prices (zero `none`); every
      new row cited with URL + date; meta bumped.
- [ ] Report/doctor/post-commit render the identical list; report output
      byte-identical across runs on the same ledger.
- [ ] CI workflow idempotent, publish workflow untouched.
- [ ] Tree review-ready; zero commits.
