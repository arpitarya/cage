> **Archived — shipped in v0.9 (see [CHANGELOG](../../CHANGELOG.md)). History, not spec.**

# Claude Code prompt: Ledger scale — partitions, scope, team aggregation

You are implementing the Cage ledger-scale work: month-partitioned ledger files,
an optional counts-safe `scope` key, and `refs/notes`-based team aggregation. The
full spec is in `docs/handoff-ledger-scale.md` and the design-of-record amendment
is `docs/cage-plan-3.6-ledger-scale.md` — **read both first** and treat the
handoff's Definition of Done (§2) and Non-negotiables (§6) as binding.

## Context to load first
- Read: `docs/cage-plan-3.6-ledger-scale.md`, `docs/handoff-ledger-scale.md`.
- Read before coding: `cage/ledger.py`, `cage/schema.py`, `cage/constants.py`,
  `cage/notessync.py`, `cage/originrecord.py`, `cage/tasks.py`, `cage/paths.py`,
  `cage/cli.py`, `cage/clicmds.py`.
- Follow existing patterns: append-only + tolerate-truncated-tail (`ledger.py`),
  fail-open write path (`ledger.append` returns `False`, never raises),
  merge-by-row-id (`notessync.merge_rows`), buffer→notes CI-sole-writer (§3.5,
  `notessync.py`), no-flag byte-identity (`matrix --human`).
- Respect `CLAUDE.md` — especially: $0/stdlib-only, determinism, `method` is
  sacred, four-agents-always, ledger-never-rewritten.

## Task
Implement, in three coherent slices (land and verify each before the next):
1. **(a) Month partitioning (calls/receipts only) + reserve `actor`** — writers
   append to `calls-YYYY-MM.jsonl` and `receipts-YYYY-MM.jsonl` using the row's own
   `ts`; readers glob `calls-*.jsonl` + legacy `calls.jsonl` and concatenate;
   `--since` skips shards whose month is entirely below `ledger.since_cutoff`. Add
   `PARTITION_GRANULARITY="month"` to `constants.py`. Do NOT partition
   `provenance.jsonl`. **Do NOT partition `tasks.jsonl`** — it is last-write-wins
   by id and a month-straddling task would split across shards (correctness
   hazard); take an `if kind in ("calls","receipts")` branch and leave tasks
   single-file. In the same schema pass, reserve `actor: str = ""` on `make_call`
   (PII-guarded — never a raw email; always empty in v1) so per-person rollup needs
   no second contract change later.
2. **(b) `scope` field** — add `scope: str = ""` to `make_call`/`make_receipt`,
   append to `CALL_FIELDS`/`RECEIPT_FIELDS`; resolve from the existing `tasks.py`
   top-level-dir git shell-out (fail-open, no new git path). Add optional
   `--scope <dir>` to `report`/`attrib`/`budget`/`matrix`; absent ⇒ identical
   output to today.
3. **(c) Team aggregation** — add `cage ledger-sync` mirroring `cage notes-sync`'s
   structure (dry-run default; writes `refs/notes/cage-ledger` only under
   `CAGE_NOTES_WRITE=1`). Do NOT call `notessync.merge_rows` verbatim — its
   tie-break ranks on `PROVENANCE_METHOD_TRUST` and `plan()` reads the provenance
   buffer by `sha`. Extract the pure union-by-id core into a shared helper; ledger
   rows use plain first-by-id (no method rank). Use a **single
   `refs/notes/cage-ledger`** ref for mixed `c_`/`r_` rows (mirrors
   `cage-provenance`); leave a `# v2:` marker to split into per-type refs only if
   partial-fetch is needed. Add `--team` to `report`/`attrib` to read the merged
   ref, falling back to local when the ref is empty/missing. Default rollup
   dimension is `scope`; per-person rollup is deferred (the `actor` field is
   reserved but unpopulated in v1).
4. **(d) Ledger-size warning** — on the `ledger` read path, sum the byte size of
   the globbed shards and, when it exceeds the threshold, print ONE warning line
   to **stderr** (never stdout) naming the size and the remedy (archive old
   `*-YYYY-MM.jsonl` shards / `ledger-sync`). Threshold resolves policy-first:
   `policy.toml [ledger] warn_mb`, else `constants.LEDGER_WARN_MB` (default set
   well above a heavy solo-year — ≈250–500 MB — so it only flags a genuine
   runaway, NOT a flat 50; confirm the value with me). Warn-only, fail-open: never
   block, never raise, swallow a stat error. Fire at most once per command
   invocation. Block mode is explicitly out on the read/derive path (flux
   invariant).

## Required workflow
1. **Explore** the listed files before writing anything; confirm the real shapes
   of `ledger.read`, `since_cutoff`, `merge_rows`, and the `tasks.py` resolver.
2. **Plan** — post a step list and the exact files you'll change for all four
   slices, and PAUSE for my confirmation before implementing.
3. **Implement incrementally** — slice (a), then (b), then (c), then (d); keep
   `just test` green between slices.
4. **Update docs to match** — README ("What's new" + bump `__version__` + refresh
   tests-passing count), CHANGELOG, docstrings (`make_call` incl. `scope`+`actor`,
   `make_receipt` incl. `scope`, `ledger-sync` help, `[ledger] warn_mb` in bundled
   `data/policy.toml`), fold the §3.6 amendment into `docs/cage-plan.md`, and fold
   team **aggregation** into the existing `cage query` `data-flow` concept (NO new
   standalone partitioning concept). For **CLAUDE.md**: the data-flow box,
   Substrate/Constants notes, and read-surface list go stale — PROPOSE those edits
   and flag them for my review; do NOT silently rewrite CLAUDE.md. Add an ADR
   capturing the "notes, not external sink" decision + its veto condition.
5. **Verify** — run `just test` and `just demo`. `demo` must still reproduce the
   §4.4 tables unchanged. Fix what you break. Do not report done until green.

## Constraints (hard)
- Use: stdlib `glob`/`datetime`, git via shell-out only, the shared union-by-id
  helper extracted from `notessync`, and the `tasks.py` resolver. Do NOT use: any
  new dependency, any external store/service/object-store/collector, any `import`ed
  git library.
- Determinism: shard names from row `ts`, no clock/random on read path; same
  ledger + policy ⇒ byte-identical tables.
- Ledger never rewritten: new writes target dated files only; no migration that
  rewrites history.
- `method` is sacred: do not change any `measured/modeled/estimated` tagging;
  aggregation is a row union, not a re-derivation.
- Four agents always: any wiring fans out to Claude Code · Codex · Copilot · Kiro.
- No-flag byte-identity: with no new flags and only legacy files, output is
  identical to pre-change. The size warning is stderr-only — stdout tables stay
  byte-identical, and determinism tests must pass with stderr ignored.
- PII: `scope` is top-level dir only; team aggregate must not default to
  per-person identity.
- Do not modify: `provenance.jsonl` partitioning, the attribution method ladder,
  the `prices.py` `est_cost_usd` fallback. The only `policy.toml` change permitted
  is the additive `[ledger] warn_mb` key (default mirrored in `constants.py`);
  touch no existing policy fields.

## Acceptance criteria (self-check before finishing)
- [ ] Partitioned write/read works; legacy single-file still read; `--since` skips
      below-cutoff shards.
- [ ] `scope` round-trips and serializes; `--scope` filters; absent-flag output
      byte-identical.
- [ ] `ledger-sync` dry-run prints plan and does not write without
      `CAGE_NOTES_WRITE`; the shared union-by-id helper unions call/receipt rows.
- [ ] `--team` reads merged ref and falls back when empty.
- [ ] Size warning fires to stderr above threshold, silent below, stdout
      unchanged either way; `[ledger] warn_mb` overrides the constant fallback; a
      stat error is swallowed.
- [ ] `just test` green (count refreshed in README + CLAUDE.md); `just demo`
      reproduces §4.4 unchanged.
- [ ] Docs updated (README/CHANGELOG/plan/docstrings/ADR); CLAUDE.md edits
      proposed for review, not auto-applied.

## Tests
Add/extend tests for: partition target by `ts`; reader globs legacy + dated;
`--since` shard skipping; `scope` round-trip + `--scope` filter + absent-flag
byte-identity; `ledger-sync` dry-run vs `CAGE_NOTES_WRITE` write; the shared
union-by-id helper on call/receipt rows; `--team` merged-read + empty-ref
fallback; size warning above/below threshold (stderr only, stdout unchanged,
policy override beats constant, stat-error swallowed); determinism (same ledger ⇒
identical stdout tables). Run with `just test`.

## Guardrails
- Ask before: changing any public schema field beyond the additive `scope`/`actor`,
  altering existing `policy.toml` fields (the additive `[ledger] warn_mb` is fine),
  touching the attribution method tagging, or any irreversible git-notes write.
- The four debated questions are DECIDED in handoff §10 (tasks not partitioned;
  `actor` reserved now, rollup deferred; single notes ref; aggregation folded into
  the `data-flow` concept) — follow them, don't re-litigate. The ONE still-open
  item is the size-warning threshold value/basis (§10) — confirm it WITH ME before
  building slice (d). If any requirement is ambiguous or conflicts with the code,
  STOP and ask rather than guessing.
