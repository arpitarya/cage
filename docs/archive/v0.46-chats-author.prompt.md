> **ARCHIVED — implemented and released in v0.46.0, 2026-08-03.** History, not spec.
> **Progress: 100%** — the single phase is built and green (1462/0). Its Phase-0
> REV-TS gate was verified independently before any work. Living spec: FORMULAS
> §2.13/§2.14, PLAN §3.5, `cage query chats-view`.

# Claude Code prompt: CHATS-AUTHOR — `agent%` on `cage insights chats`

**Model:** Opus — touches the provenance substrate (one deliberate deviation from
a standing convention), reconciles against a second derivation (`commitview`)
without duplicating it, and answers three standing guards; a wrong call here
publishes a wrong authorship number under a friendly column head.

**Progress:** 0% — single phase behind a hard gate, not started.

You are implementing the `agent%` authorship column on `cage insights chats` —
per chat, the share of landed matchable lines (in files that chat proposed) that
matched the agent's own proposals. The full spec is
[docs/chats-author.handoff.md](v0.46-chats-author.handoff.md); the design of record is
[docs/proposals/chats-agent-authorship-column.proposal.md](v0.46-chats-author.proposal.md).
Read both first — Definition of Done and Non-negotiables are binding.

## Gate — verify before ANY work

REV-TS ([archived proposal](v0.45-rev-ts.proposal.md) ·
[handoff](v0.45-rev-ts.handoff.md)) must already be implemented:
`commitjoin` compares only normalized UTC timestamp strings, and a non-UTC (+05:30)
fixture exists and passes. **If it has not landed, STOP and report — do not build this
column on the skewed join, and do not fix REV-TS yourself** (it is its own filed
program).

**Gate status 2026-08-02: PASSES.** It failed once (no normalizer at HEAD), REV-TS was
then built and archived the same day, and the fixtures live in
`tests/test_authorship_capture.py` — **not** `goldenseed.py`, because a golden asserts
nothing here (`commitview._date` slices the offset away). Verify it yourself rather
than trusting this line; the four to look for are named
`*_non_utc_*` / `*_mixed_offset_*` / `*_inclusive_upper_bound_holds_on_a_non_utc_bound`.

## Context to load first

- `CLAUDE.md` (laws: determinism, method, honesty, $0/stdlib, docs discipline).
- `cage/chats.py` — the view you extend (bucket key, footer discipline, CSV twin).
- `cage/authorcapture.py` step 4 — where `residual_lines` is computed (diff and
  landed paths already in hand); `COVERAGE_GAPS` / `coverage_note()`.
- `cage/schema.py` `make_provenance` + `PROVENANCE_COUNT_FIELDS` — the omit-at-0
  loop you deviate from, explicitly; `cage/originrecord.py` `record`'s `**counts`
  filter (the closed boundary the new key must pass).
- `cage/commitview.py` `_buckets` — the per-commit math you reconcile with and
  must NOT re-implement or modify; `cage/agents.py` `row_surface`;
  `cage/linematch.py` (`matchable`/`normalize` — ONE function, both sides).

## Task

Per the handoff: add `residual_lines` (always written, including 0, on new
transcript rows — presence of the key is the version gate) · join provenance rows
into `chats.summarize` by `(row_surface(agent), session_id)` · render one
`agent%` column (default on, after `credits`) with the three refusal shapes as
footnoted `—`, never 0% · CSV `agent_lines`/`residual_lines`/`agent_pct` with
empty-when-refused cells · extend the money-independence test and add the
no-USD-combination guard.

## Required workflow

1. **Gate first** (above). Then **explore** the named files — confirm the join key
   (`session=f.stem` on both sides) and the counts filter behavior yourself.
2. **Plan** — list files you'll change and the test list; pause for my
   confirmation before implementing.
3. **Implement incrementally** — schema+capture → join → render/CSV → tests green
   at each step.
4. **Update docs to match** (handoff §9.5): FORMULAS §2.13+§2.14, both explain
   entries, README, CHANGELOG, PLAN, GLOSSARY, DOC-REGISTRY bumps, OPEN-WORK row
   removal after IMPLEMENTATION.md records the milestone. **For CLAUDE.md: propose
   the edit and flag it for Arpit's review — do not silently rewrite it.** Archive
   the pair + proposal to `docs/archive/vX.Y-chats-author.*` on green and update
   `proposals/README.md`.
5. **Verify**: `just test` — full suite 0 fail (from 1401/0); goldens blessed
   (`CAGE_BLESS_GOLDENS=1`, new fixtures only). Do not report done before that.

## Constraints (hard)

- stdlib only; derive-time only; the ledger is never rewritten; frozen provenance
  rows are never backfilled or migrated — pre-upgrade rows render `—` forever.
- No rate, no minutes, no USD ever touches an authorship number (the v0.36 law);
  `agent%` never combines with `cost` in any formula.
- No acceptance rate (`kept/suggested`) anywhere; no re-matching at render —
  counts are READ from rows; line bodies stay transient (ADR 0008 — the
  plant-string test stays green, untouched).
- `—` never means 0 and never enters CSV; every refusal shape carries its
  footnoted reason; coverage gaps named via `coverage_note()`, not invented text.
- Do not modify: `commitview.py` · `verbmap` · wiring modules · the REV-TS code
  beyond reading it.

## Acceptance criteria (self-check before finishing)

- [ ] Every Definition-of-Done box in the handoff checks true, gate included.
- [ ] A zero `residual_lines` survives the row round-trip (the deviation is real
      and tested, not accidental).
- [ ] Chat sums reconcile with §2.14 per-commit buckets on the goldenseed session.
- [ ] Money-independence: deleting `provenance.jsonl` leaves every pre-existing
      cell byte-identical.
- [ ] Docs per §9.5; CLAUDE.md edit proposed, not applied; IMPLEMENTATION.md +
      WORKLOG appended; OPEN-WORK row deleted; pair + proposal archived.

## Tests

Extend `tests/test_chats.py` (join math · refusal triad · money-independence ·
no-USD guard · CSV parity · determinism) and the capture tests (residual
arithmetic incl. the zero case). Golden fixtures per state. Run `just test`.

## Guardrails

- Ask before: touching anything on the do-not-modify list, adding any flag not
  named here, demoting the column behind `--authorship` (allowed only if the
  100-col golden forces it — say so), or archiving docs early.
- If the join key, the counts filter, or the gate check contradicts what the
  handoff claims, STOP and ask — do not improvise a second convention.
