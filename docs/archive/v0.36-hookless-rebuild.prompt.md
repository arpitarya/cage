> **Archived — implemented for v0.36 (unreleased: the work is built + green, the release is pending). See [CHANGELOG](../../CHANGELOG.md) and [IMPLEMENTATION](../IMPLEMENTATION.md). History, not spec.**

# Claude Code prompt: hookless rebuild + import-ledger plan (Phases 0–4, whole plan)

**Model:** **Opus** — Phase 0 completes a deletion with hidden entanglements
(doctor, the explain registry, ~15 test files that encode hook behavior),
Phases 1–3 change the substrate contract (`schema.make_call` additive fields),
and Phase 4 is a correlation heuristic where the method-tagging judgment *is* the
work. All land in the Opus row of CLAUDE.md's model rubric ("deleting code with
entanglements", "any change to the substrate contract", "diagnosis is the work").
Do not run this on Sonnet or Haiku.

---

You are finishing cage's conversion to **pull-only capture** (no hooks, no rendered
md assets — `cage import` + capture-on-read + MCP only), then implementing **all**
of `docs/cage-import-ledger-plan.md` (Phases 1–4). The full spec is
`docs/hookless-rebuild.handoff.md` — read it first and treat its Definition of Done
and Non-negotiables as binding. **The repo is mid-surgery and currently does not
import** (`wiringscan.py` is half-edited); Phase 0 is blocking.

## Context to load first

1. `CLAUDE.md` (repo law — note: several sections describe the pre-removal world;
   the handoff's §9.5 covers proposing updates)
2. `docs/hookless-rebuild.handoff.md` (this task's spec — §4 is the exact
   done/remaining change-map)
3. `docs/cage-import-ledger-plan.md` (design of record for Phases 1–3)
4. Then the code the change-map names: `cage/wiringscan.py`, `cage/doctorcmd.py`,
   `cage/explain_data.py`, `cage/schema.py`, `cage/importcmd.py`,
   `cage/transcript.py`, `cage/ledger.py`, `cage/graphifymeter.py`
5. Sample inputs + byte-checked expected rows: `samples/agent-artifacts/**`

## Task

- **Phase 0 (blocking):** execute handoff §4 "REMAINING" items 1–9 exactly; suite
  green before anything else.
- **Phase 1:** additive `surface`/`cache_write_in`/`premium`/`import_id` call-row
  fields; per-agent × surface import summary with UNPRICED flags; price at import
  via `prices.call_usd` (plan §2.1–§2.2).
- **Phase 2:** `savings/<tool>/savings-YYYY-MM.jsonl` tree + `savings.record()`;
  graphify routes there; report unions legacy receipts (plan §3).
- **Phase 3:** `imports.jsonl` capture manifest from `importcmd.run` and
  `graphifymeter._meter`; thread `import_id` (plan §4).
- **Phase 4 (gated):** best-effort `task` backfill correlating imports against
  `tasks.jsonl` by session/time window — mirror the existing `taskgroup` join
  (task-id first, session-window fallback, overlaps → smallest task id), tag it
  with its own method/confidence, put a blocking min-n gate on it. Build + test it;
  it **activates only when validated against real correlated data** — absent that
  data, ship it disabled-by-default and record the activation gate in the plan's
  decisions log (handoff §3).

## Required workflow

1. **Explore** — read the handoff change-map against the actual tree; verify the
   "already DONE" list matches reality before trusting it (`git status`, imports).
2. **Plan** — post the file-level plan for Phase 0 and pause for confirmation;
   then per-phase plans as you go.
3. **Implement incrementally** — commit at each green checkpoint: Phase 0 test-green
   is its own commit; each plan phase its own commit(s). Never mix removal and
   feature work in one commit.
3.5. **Log every milestone in `IMPLEMENTATION.md`** (standing CLAUDE.md rule): after
   each green checkpoint / commit / phase step, append an entry — date · milestone ·
   implemented · files · tests · next — newest first. A finished milestone with no
   entry is unrecorded work, same class of bug as a missing changelog entry.
4. **Update docs to match** — README, docs/README.md index, CHANGELOG +
   `__version__` (repo release rule), the ADR from handoff §9.5. For **CLAUDE.md:
   propose the edits and flag them for Arpit's review — do not silently rewrite it.**
5. **Verify** — `python -m pytest -q` (update the README test count), scratch-repo
   smoke (`cage setup --claude && cage import && cage report`), goldens re-blessed
   only where the output change is intended, `python -m build` packaging check.

## Constraints (hard)

- stdlib only, `dependencies = []`; fail-open write paths; determinism (no
  clocks/random in derived views); `method` law (a reconstructed number is never
  `measured`); counts-never-content PII (tilde-relative `source_path`, no prompt
  text, `tool` dir names are validated closed tokens).
- All new row fields **additive-optional**: absent ⇒ byte-identical legacy row.
- Do NOT reintroduce hooks, an OS scheduler, or skill/steering rendering. Do NOT
  drop the three dead-wiring mitigations: `verbmap.REMOVED` hook-* entries,
  wire-module heal-by-removal, wiringscan leftover detection.
- Do not modify: `docs/archive/`, `docs/adr/` (except the new ADR),
  `docs/regression/`, `samples/**` inputs, `.github/workflows/publish.yml`.
- `copilot/auto` and kiro `agent` render `UNPRICED`, never `$0`. Claude `surface`
  stays `""` (shared store — honest, not a TODO).

## Acceptance criteria (self-check before finishing)

- [ ] Every box in handoff §2 checked
- [ ] `pytest -q` green; README count refreshed
- [ ] `IMPLEMENTATION.md` has one entry per milestone actually hit (no gaps)
- [ ] `cage hook-stop` prints a direction and exits 1
- [ ] Phase 4 correlation never writes `task` without its method/confidence tags;
      disabled-by-default unless validated on real data
- [ ] Legacy ledgers and the fixture corpus parse unchanged
- [ ] `cage demo` still reproduces the plan §4.4 tables
- [ ] CHANGELOG entry written; handoff/prompt pair moved to `docs/archive/` in the
      shipping change; CLAUDE.md edits proposed (not applied)

## Tests

Extend `derived-rows.json` fixtures with the new additive fields; add per-agent
`savings/` and `imports.jsonl` fixtures; assert the import summary against a
hand-derived reference; keep `tests/test_cli_tiering.py` grep gates green.
Command: `python -m pytest -q`.

## Guardrails

- Ask before deleting any test whose purpose is unclear — delete only when the
  pinned behavior was itself removed.
- Ask before changing any public CLI verb or exit code beyond the spec.
- If the handoff's "already DONE" list disagrees with the tree, STOP and report
  the diff rather than guessing.
- The three open questions in handoff §10 need answers before their respective
  steps: release split (before tagging), `session_name` flag (before Phase 3),
  receipt migration (before Phase 2 ships).
